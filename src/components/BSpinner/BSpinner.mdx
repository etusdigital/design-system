import { Canvas, Meta } from '@storybook/blocks';
import * as BSpinnerStories from './BSpinner.stories.ts';

<Meta of={BSpinnerStories} />

# BSpinner

<Canvas of={BSpinnerStories.Primary} />

## Color

The color can be changed through the **color** CSS property.

<Canvas of={BSpinnerStories.Colors} />

## Size

The size can be changed through the **font-size** CSS property, or you can manipulate **width** and **height** directly.

<Canvas of={BSpinnerStories.Sizes} />
