// Layout Module - Layout and structure components
// Collapse, containers, etc.

// Re-export from legacy components until migration is complete
export { default as Collapse } from '../../components/BCollapse';

// Export types
export type { BCollapseProps } from '../../components/BCollapse/BCollapse.types';