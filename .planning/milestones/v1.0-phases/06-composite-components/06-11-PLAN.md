---
phase: 06-composite-components
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Pagination/Pagination.stories.tsx
  - src/components/Stepper/Stepper.stories.tsx
  - src/components/Carousel/Carousel.stories.tsx
  - src/components/RoundMenu/RoundMenu.stories.tsx
autonomous: true
gap_closure: true
requirements: [COMP-08, COMP-09, COMP-10, COMP-11]

must_haves:
  truths:
    - "Pagination story renders numbered page buttons in Storybook"
    - "Stepper story renders step indicators with circle icons in Storybook"
    - "Carousel story renders slide content with navigation in Storybook"
    - "RoundMenu story renders radial menu items in Storybook"
  artifacts:
    - path: "src/components/Pagination/Pagination.stories.tsx"
      provides: "React TSX Storybook story for Pagination"
    - path: "src/components/Stepper/Stepper.stories.tsx"
      provides: "React TSX Storybook story for Stepper"
    - path: "src/components/Carousel/Carousel.stories.tsx"
      provides: "React TSX Storybook story for Carousel"
    - path: "src/components/RoundMenu/RoundMenu.stories.tsx"
      provides: "React TSX Storybook story for RoundMenu"
  key_links:
    - from: "Pagination.stories.tsx"
      to: "Pagination component"
      via: "import { Pagination } from './Pagination'"
      pattern: "from '@storybook/react'"
---

<objective>
Convert four Vue story files (Pagination, Stepper, Carousel, RoundMenu) to React TSX format so Storybook can render them.

Purpose: Closes UAT gaps for tests 5, 6, 12, and 13. All four are self-contained interactive components with no cross-dependencies.
Output: Four new .stories.tsx files, four old .stories.ts files deleted, any legacy .mdx files in these directories removed.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-composite-components/06-02-SUMMARY.md

<interfaces>
<!-- React story pattern — exact structure to follow -->
<!-- Reference: src/components/Dialog/Dialog.stories.tsx -->

Conversion rules (Vue → React):
- `@storybook/vue3` → `@storybook/react`
- `import Foo from './Foo.vue'` → `import { Foo } from './Foo'`
- `v-model="args.x"` → `value={x} onChange={setX}` (with useState inside render)
- `:prop="val"` → `prop={val}`
- `@event="fn"` → `onEvent={fn}`
- Import Button/Card/MetricCard/Icon from their respective paths if needed in story renders

PaginationProps (check Pagination.tsx for interface):
  value?: number; defaultValue?: number; onChange?: (v: number) => void; length?: number;

StepperProps (check Stepper.tsx — NOTE: version prop dropped entirely per STATE.md decision):
  value?: any; defaultValue?: any; onChange?: (v: any) => void;
  options?: Array<{label: string; value: string; icon?: string}>;
  labelKey?: string; valueKey?: string; size?: 'medium' | 'large';
  disabled?: boolean; allowedSkip?: boolean; background?: string; getObject?: boolean;
  onChangeStep?: (option: any, index: number) => void;

CarouselProps (check Carousel.tsx for interface):
  value?: number; defaultValue?: number; onChange?: (v: number) => void;
  options?: any[]; visible?: number; interval?: number;
  disabled?: boolean; circular?: boolean; vertical?: boolean;
  children?: (option: any, index: number) => React.ReactNode;  (render prop for slot)

RoundMenuProps (check RoundMenu.tsx for interface):
  options?: Array<{icon: string; label: string; background: string; action: () => void}>;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Pagination and Stepper stories to React TSX</name>
  <files>
    src/components/Pagination/Pagination.stories.tsx,
    src/components/Stepper/Stepper.stories.tsx
  </files>
  <action>
    Read the actual Pagination.tsx and Stepper.tsx component files to confirm their prop interfaces before writing stories.

    **Pagination.stories.tsx** — stories to create:
    - `Primary`: useState(1) for value, Pagination with value={page} onChange={setPage} length={10}

    **Stepper.stories.tsx** — stories to create:
    - `Primary`: useState("basic-info") for value, Stepper with the 4-step options array (Basic Info/Configuration/Review/Complete with icons), size/disabled/allowedSkip/getObject controls
    - `Sizes`: Render two Stepper instances side by side in a flex-col div — one size="medium", one size="large"
    - `Disabled`: disabled={true}
    - `AllowedSkip`: allowedSkip={true}

    Note per STATE.md: The React Stepper drops the `version` prop entirely — only v2 circle-icon-with-connectors rendered. Do not include a version prop or Versions story.

    After creating both .stories.tsx files, delete the old .stories.ts files:
    - Delete `src/components/Pagination/Pagination.stories.ts`
    - Delete `src/components/Stepper/Stepper.stories.ts`

    Also delete Pagination.mdx and Stepper.mdx if they exist.
  </action>
  <verify>
    ls src/components/Pagination/Pagination.stories.tsx src/components/Stepper/Stepper.stories.tsx
    ls src/components/Pagination/Pagination.stories.ts 2>/dev/null || echo "deleted"
    ls src/components/Stepper/Stepper.stories.ts 2>/dev/null || echo "deleted"
  </verify>
  <done>Pagination.stories.tsx and Stepper.stories.tsx created. Old .stories.ts files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Carousel and RoundMenu stories to React TSX</name>
  <files>
    src/components/Carousel/Carousel.stories.tsx,
    src/components/RoundMenu/RoundMenu.stories.tsx
  </files>
  <action>
    Read the actual Carousel.tsx and RoundMenu.tsx files to confirm their prop interfaces. Pay special attention to how Carousel handles the slot/render-prop for its option template — the Vue version uses `<template #option="{ option, index }">` which must become a render prop or children in the React version.

    **Carousel.stories.tsx** — stories to create:
    - `Primary`: useState(0) for value, Carousel with the 5-item campaign metrics options array. The render prop for each slide should show a Card with the metric data in a simplified form — a div with the URL and metric values displayed as text is acceptable (no need to import MetricCard — just render a simple div showing option.url and the key metrics as text)
    - `Interval`: circular={true}, interval={3000}
    - `Circular`: circular={true}
    - `Disabled`: disabled={true}
    - `Vertical`: vertical={true}

    **RoundMenu.stories.tsx** — stories to create:
    - `Primary`: No state needed (no value prop). Render RoundMenu inside a div with className="px-[3em] py-[4em]", pass the 9-item options array (email, stop, schedule, history, add, new_label, label_off, add_circle, call_split with their labels, backgrounds, and action:()=>{} callbacks).

    After creating both .stories.tsx files, delete the old .stories.ts files:
    - Delete `src/components/Carousel/Carousel.stories.ts`
    - Delete `src/components/RoundMenu/RoundMenu.stories.ts`

    Also delete Carousel.mdx and RoundMenu.mdx if they exist.
  </action>
  <verify>
    ls src/components/Carousel/Carousel.stories.tsx src/components/RoundMenu/RoundMenu.stories.tsx
    ls src/components/Carousel/Carousel.stories.ts 2>/dev/null || echo "deleted"
    ls src/components/RoundMenu/RoundMenu.stories.ts 2>/dev/null || echo "deleted"
  </verify>
  <done>Carousel.stories.tsx and RoundMenu.stories.tsx created. Old .stories.ts files deleted.</done>
</task>

</tasks>

<verification>
- All four .stories.tsx files exist with `@storybook/react` imports
- All four old .stories.ts files deleted
- No .mdx files remaining in Pagination/, Stepper/, Carousel/, RoundMenu/ directories
</verification>

<success_criteria>
Pagination, Stepper, Carousel, and RoundMenu each have a `.stories.tsx` file using React JSX render functions. All legacy Vue `.stories.ts` files removed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-11-SUMMARY.md`
</output>
