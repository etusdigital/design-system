---
phase: 06-composite-components
plan: 27
type: execute
wave: 2
depends_on: [24]
files_modified:
  - src/components/Dropdown/Dropdown.tsx
  - src/components/Dropdown/Dropdown.module.css
  - src/components/Dropdown/Dropdown.stories.tsx
autonomous: true
requirements: [COMP-05]
gap_closure: true

must_haves:
  truths:
    - "Dropdown clicking an item selects it and updates display"
    - "Dropdown groups show as clickable rows with chevron_right that reveal nested sub-options in a flyout card to the right"
  artifacts:
    - path: "src/components/Dropdown/Dropdown.tsx"
      provides: "Flyout submenu for groups, working selection"
    - path: "src/components/Dropdown/Dropdown.module.css"
      provides: "Flyout card positioned absolute left:100% top:0"
  key_links:
    - from: "DropdownOption group branch"
      to: "flyout card"
      via: "local useState for subExpanded, absolutely positioned card"
      pattern: "subExpanded.*useState"
---

<objective>
Fix Dropdown item selection and implement flyout submenu for groups.

Purpose: Dropdown stories use controlled value without onChange feedback, freezing selection. Groups render as flat inline indented list instead of Vue's flyout submenu pattern with chevron_right and absolutely-positioned sub-card. UAT round 3 test 7 (blocker).
Output: Working Dropdown selection, flyout group submenus.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Dropdown/Dropdown.tsx
@src/components/Dropdown/Dropdown.module.css
@src/components/Dropdown/Dropdown.stories.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Dropdown selection and implement flyout group submenus</name>
  <files>src/components/Dropdown/Dropdown.tsx, src/components/Dropdown/Dropdown.module.css</files>
  <action>
Two fixes:

1. **Fix selection** (Dropdown.tsx): The useControllable marks value as controlled when a value prop is passed, which skips setInternalValue. Fix by ensuring the Dropdown component properly handles both controlled and uncontrolled modes:
   - If stories pass `value` without `onChange`, they should use `defaultValue` instead for uncontrolled mode
   - OR ensure the Dropdown internal onSelect handler calls both the external onChange AND updates internal state
   - The simpler fix: make the onSelect handler always call setModel (from useControllable) which handles both modes correctly

2. **Implement flyout submenu for groups** (Dropdown.tsx + Dropdown.module.css): Replace the flat inline group rendering in DropdownOption (lines 49-64) with Vue's flyout pattern:

   In DropdownOption, when option.options exists:
   - Add local `const [subExpanded, setSubExpanded] = useState(false)`
   - Render a clickable row (not a static header) with the group label and a chevron_right Icon on the right
   - On click/hover of the group row, toggle subExpanded
   - When subExpanded is true, render a flyout card: a new absolutely-positioned div with `position: absolute; left: 100%; top: 0;` containing the nested DropdownOptions
   - The flyout card should have the same styling as the main dropdown card (bg-neutral-surface-default, shadow, border, rounded)
   - The parent group row needs `position: relative` so the flyout positions correctly

   CSS additions to Dropdown.module.css:
   - `.groupRow { position: relative; display: flex; align-items: center; justify-content: space-between; cursor: pointer; padding: ... }`
   - `.groupRow:hover { background: var(--neutral-surface-hover); }`
   - `.flyoutCard { position: absolute; left: 100%; top: 0; min-width: 160px; z-index: 90; margin-left: var(--spacing-xxs); }`
   - Style the flyout card with same bg/shadow/border/rounded as main dropdown

   Remove the old .groupHeader class and its flat-list rendering.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "subExpanded\|flyout\|chevron_right\|groupRow" src/components/Dropdown/Dropdown.tsx src/components/Dropdown/Dropdown.module.css | head -10</automated>
  </verify>
  <done>Clicking item selects it. Groups show as clickable rows with chevron revealing flyout sub-card to the right.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Dropdown stories for proper selection feedback</name>
  <files>src/components/Dropdown/Dropdown.stories.tsx</files>
  <action>
Update Dropdown stories that pass controlled `value` prop:
- Change from `value={someValue}` without onChange to using `defaultValue` for uncontrolled mode
- OR add proper useState + onChange for controlled stories that need it
- Ensure at least one story demonstrates nested group options with the flyout submenu (e.g., a "Publisher" group with "Group Account" and "Account" sub-options)
- Add a story variant "WithGroups" that shows the flyout behavior if not already present

Example group options structure:
```typescript
const groupOptions = [
  { label: 'Direct', value: 'direct' },
  { label: 'Publisher', options: [
    { label: 'Group Account', value: 'group-account' },
    { label: 'Account', value: 'account' },
  ]},
];
```
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "defaultValue\|WithGroup\|Publisher" src/components/Dropdown/Dropdown.stories.tsx | head -5</automated>
  </verify>
  <done>Dropdown stories use correct controlled/uncontrolled patterns. Group flyout story exists.</done>
</task>

</tasks>

<verification>
- Dropdown story: click an option, it gets selected and displayed
- Dropdown WithGroups story: groups show chevron, clicking reveals flyout sub-card to the right
</verification>

<success_criteria>
- Dropdown item selection works in stories
- Groups render with flyout submenu pattern matching Vue behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-27-SUMMARY.md`
</output>
