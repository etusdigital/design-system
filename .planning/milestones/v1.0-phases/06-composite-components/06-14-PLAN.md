---
phase: 06-composite-components
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DatePicker/DatePicker.stories.tsx
  - src/components/Navbar/Navbar.stories.tsx
  - src/components/ColorPicker/ColorPicker.stories.tsx
autonomous: true
gap_closure: true
requirements: [COMP-01, COMP-02, COMP-04]

must_haves:
  truths:
    - "DatePicker story renders a date filter popover with Calendar and action buttons in Storybook"
    - "Navbar story renders navigation items with Dropdown submenus and profile slot in Storybook"
    - "ColorPicker story renders the color canvas with hue/opacity sliders in Storybook"
  artifacts:
    - path: "src/components/DatePicker/DatePicker.stories.tsx"
      provides: "React TSX Storybook story for DatePicker"
    - path: "src/components/Navbar/Navbar.stories.tsx"
      provides: "React TSX Storybook story for Navbar"
    - path: "src/components/ColorPicker/ColorPicker.stories.tsx"
      provides: "React TSX Storybook story for ColorPicker"
  key_links:
    - from: "DatePicker.stories.tsx"
      to: "DatePicker component"
      via: "import { DatePicker } from './DatePicker'"
      pattern: "from '@storybook/react'"
---

<objective>
Convert three Vue story files (DatePicker, Navbar, ColorPicker) to React TSX format — the final batch of composite component stories.

Purpose: Closes UAT gaps for tests 15, 16, and 17. DatePicker uses calculateDate utility from src/utils; Navbar uses slot-based logo/actions composition.
Output: Three new .stories.tsx files, three old .stories.ts files deleted, any legacy .mdx files removed.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-composite-components/06-08-SUMMARY.md
@.planning/phases/06-composite-components/06-09-SUMMARY.md

<interfaces>
<!-- React story pattern — exact structure to follow -->
<!-- Reference: src/components/Dialog/Dialog.stories.tsx -->

Conversion rules (Vue → React):
- `@storybook/vue3` → `@storybook/react`
- `import Foo from './Foo.vue'` → `import { Foo } from './Foo'`
- `v-model` → `value + onChange` with useState
- `v-model:expanded` → `expanded + onExpandedChange` with useState
- `v-model:type` → `type + onTypeChange` with useState
- Vue template slots → React children or named props

DatePickerProps (check DatePicker.tsx for exact interface — key props):
  value, defaultValue, onChange,
  type ('date' | 'period' | 'compare'), onTypeChange,
  expanded, onExpandedChange,
  labelValue, lang, allowChangeType, minDate, maxDate,
  options (array of {value, label, selected?, calculate: () => Date | Date[]}),
  disabled, required, hideActions, isError, errorMessage,
  absolute, alignRight, separator,
  children (placeholder trigger text),
  clearLabel, applyLabel, compareLabel (string props for action labels)

IMPORTANT: DatePicker uses `calculateDate` utility from `../../utils`.
Import: `import { calculateDate } from '../../utils'`

NavbarProps (check Navbar.tsx for exact interface — key props):
  value, defaultValue, onChange,
  title, options, profile, labelKey, valueKey, getObject,
  logo (React.ReactNode — slot), children (React.ReactNode — default slot),
  actions (React.ReactNode — slot), notifications (React.ReactNode — slot)

ColorPickerProps (check ColorPicker.tsx for exact interface — key props):
  value, defaultValue, onChange,
  type ('hexa' | 'hsla' | 'hwb' | 'hsva' | 'rgba'),
  noShadow
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert DatePicker stories to React TSX</name>
  <files>src/components/DatePicker/DatePicker.stories.tsx</files>
  <action>
    First read `src/components/DatePicker/DatePicker.tsx` and `src/utils/index.ts` (or wherever calculateDate is exported) to confirm prop interfaces and the utility import path.

    **DatePicker.stories.tsx** — stories to create:
    - `Primary`: useState(undefined) for value, useState(false) for expanded, useState("date") for type. DatePicker with the 6-item preset options array (Today/Yesterday/Last 7 days/Last 15 days/Last 30 days/Last month using calculateDate). Children text "Date Filter". Props clearLabel="Clear", applyLabel="Apply", compareLabel="Compare two periods". Include lang/allowChangeType/minDate/maxDate/disabled/required/hideActions/isError/errorMessage/absolute/alignRight/separator controls.
    - `Lang`: lang="pt-BR", separator="e"
    - `AllowChangeType`: allowChangeType={true}, defaultValue={[]}, type="period"
    - `Period`: defaultValue={[]}, type="period"
    - `Compare`: defaultValue={[]}, type="compare"
    - `MinDate`: minDate={new Date()}
    - `MaxDate`: maxDate={new Date()}
    - `Absolute`: absolute={true}
    - `Disabled`: disabled={true}
    - `Required`: required={true}
    - `IsError`: isError={true}, errorMessage="Error message"
    - `AlignRight`: alignRight={true} — wrap in `<div className="flex w-full justify-end">`
    - `Separator`: defaultValue={[[], []]}, type="compare", separator="separator"

    After creating DatePicker.stories.tsx, delete `src/components/DatePicker/DatePicker.stories.ts`.
    Also delete `src/components/DatePicker/DatePicker.mdx` if it exists.
  </action>
  <verify>
    ls src/components/DatePicker/DatePicker.stories.tsx
    ls src/components/DatePicker/DatePicker.stories.ts 2>/dev/null || echo "deleted"
  </verify>
  <done>DatePicker.stories.tsx created with React imports. Old DatePicker.stories.ts deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Navbar and ColorPicker stories to React TSX</name>
  <files>
    src/components/Navbar/Navbar.stories.tsx,
    src/components/ColorPicker/ColorPicker.stories.tsx
  </files>
  <action>
    First read `src/components/Navbar/Navbar.tsx` and `src/components/ColorPicker/ColorPicker.tsx` to confirm exact prop interfaces.

    **Navbar.stories.tsx** — stories to create:
    - `Primary`: useState("dashboard") for value. Navbar with the 4-item options (Dashboard, Analytics with Reports/Metrics sub-options, Users, Settings bottom). Props: title="Navbar", labelKey="label", valueKey="value", profile={{name:"John Doe"}}, getObject={false}. Pass a notifications slot content as the React equivalent prop: `notifications={<div className="p-base"><h4 className="font-semibold mb-xs">Notifications</h4><p className="text-sm text-neutral-foreground-medium">No new notifications</p></div>}`.
    - `CustomSlots`: No state needed. Two Navbar instances showing slot composition. First: title slot `<div>Slot: Title</div>`, default children `<h4>Slot: default</h4>`, actions `<div>Slot: actions</div>`. Second (with mt-xs): logo slot `<div>Slot: logo</div>`, default children `<h4>Slot: default</h4>`, actions `<div>Slot: actions</div>`.

    **ColorPicker.stories.tsx** — stories to create:
    - `Primary`: useState("") for value. ColorPicker with value={color} onChange={setColor}, type="hexa", noShadow={false}. Include type/noShadow controls.
    - `NoShadow`: noShadow={true}

    After creating both .stories.tsx files, delete the old .stories.ts files:
    - Delete `src/components/Navbar/Navbar.stories.ts`
    - Delete `src/components/ColorPicker/ColorPicker.stories.ts`
    Also delete any .mdx files in Navbar/ and ColorPicker/ directories.
  </action>
  <verify>
    ls src/components/Navbar/Navbar.stories.tsx src/components/ColorPicker/ColorPicker.stories.tsx
    ls src/components/Navbar/Navbar.stories.ts 2>/dev/null || echo "deleted"
    ls src/components/ColorPicker/ColorPicker.stories.ts 2>/dev/null || echo "deleted"
  </verify>
  <done>Navbar.stories.tsx and ColorPicker.stories.tsx created. Old .stories.ts files deleted.</done>
</task>

</tasks>

<verification>
- All three .stories.tsx files exist with `import type { Meta, StoryObj } from '@storybook/react'`
- All three old .stories.ts files deleted
- No .mdx files remaining in DatePicker/, Navbar/, ColorPicker/ directories
</verification>

<success_criteria>
DatePicker, Navbar, and ColorPicker each have a `.stories.tsx` file using React JSX render functions. All legacy Vue `.stories.ts` files removed. All 17 composite component story files have been converted to React TSX format. All legacy .mdx conflicts removed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-14-SUMMARY.md`
</output>
