---
phase: 06-composite-components
plan: 26
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Stepper/Stepper.tsx
  - src/components/Stepper/Stepper.module.css
  - src/components/RoundMenu/RoundMenu.tsx
  - src/components/RoundMenu/RoundMenu.module.css
autonomous: true
requirements: [COMP-04, COMP-12]
gap_closure: true

must_haves:
  truths:
    - "Stepper past steps show their original icon (not checkmark) in filled green circle"
    - "Stepper allowSkip=false prevents clicking non-adjacent steps"
    - "Stepper future steps show correct visual (not solid gray disc)"
    - "RoundMenu label tooltip expands rightward without moving button origin"
    - "RoundMenu items are properly sized and spaced"
  artifacts:
    - path: "src/components/Stepper/Stepper.tsx"
      provides: "Original icon for past steps, allowSkip adjacency guard"
    - path: "src/components/Stepper/Stepper.module.css"
      provides: "Correct future step background token"
    - path: "src/components/RoundMenu/RoundMenu.tsx"
      provides: "Stable button position with rightward label expansion"
    - path: "src/components/RoundMenu/RoundMenu.module.css"
      provides: "Anchored menuItem positions, proper sizing"
  key_links:
    - from: "Stepper.tsx handleStepClick"
      to: "allowSkip prop"
      via: "adjacency guard checking Math.abs(index - current) <= 1"
      pattern: "allowSkip"
---

<objective>
Fix Stepper visual design, allowSkip enforcement, and icon rendering. Fix RoundMenu button positioning and item sizing.

Purpose: Stepper has three bugs: hardcoded checkmark for past steps (should keep original icon), missing allowSkip adjacency guard, wrong CSS token for future steps. RoundMenu has position drift when label shows and items are too small. UAT round 3 tests 4 and 10.
Output: Correct Stepper visuals and interaction, stable RoundMenu layout.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Stepper/Stepper.tsx
@src/components/Stepper/Stepper.module.css
@src/components/RoundMenu/RoundMenu.tsx
@src/components/RoundMenu/RoundMenu.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Stepper icon, allowSkip, and future step visual</name>
  <files>src/components/Stepper/Stepper.tsx, src/components/Stepper/Stepper.module.css</files>
  <action>
Three fixes:

1. **Remove past-step checkmark substitution** (Stepper.tsx ~line 116-120): The code currently renders a 'check' icon for past steps. Remove this conditional -- ALL steps (past, active, future) should always render their original icon via getIcon(option). Past steps are distinguished by their green filled background, not by icon change.

2. **Add allowSkip prop and adjacency guard** (Stepper.tsx):
   - Add `allowSkip?: boolean` to StepperProps (defaults to false if not provided).
   - In handleStepClick(index), add a guard: if allowSkip is falsy and Math.abs(index - current) > 1, return early (don't allow the click). This prevents skipping non-adjacent steps. Only adjacent steps (current +/- 1) and the current step itself should be clickable when allowSkip is false.

3. **Fix .backgroundFuture CSS** (Stepper.module.css): The `.backgroundFuture` class uses `--neutral-border-default` as background-color, making future steps appear as solid gray discs. Change to use a surface/neutral token like `--neutral-surface-default` or `--neutral-surface-secondary` so future steps appear as lighter, less prominent circles compared to past (green) and active steps. Check what token the past steps use and pick a lighter neutral surface token for future.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "check\|allowSkip\|backgroundFuture" src/components/Stepper/Stepper.tsx src/components/Stepper/Stepper.module.css | head -10</automated>
  </verify>
  <done>Past steps show original icon. allowSkip=false blocks non-adjacent clicks. Future steps have correct lighter background.</done>
</task>

<task type="auto">
  <name>Task 2: Fix RoundMenu button position anchoring and item sizing</name>
  <files>src/components/RoundMenu/RoundMenu.tsx, src/components/RoundMenu/RoundMenu.module.css</files>
  <action>
Two fixes:

1. **Pin menuItem positions** (RoundMenu.module.css + RoundMenu.tsx): The .menuItem wrapper uses position:absolute with only translate3d but no left:0/top:0 baseline anchor. When the trigger Button expands into a pill shape (showing label), the transform origin shifts because it's calculated from the growing element width.
   - Add `left: 0; top: 0;` to .menuItem CSS class so translate3d offsets from a known zero-point.
   - Add `transform-origin: left center` to the trigger button wrapper so pill expansion goes rightward while keeping the left edge (circle origin) fixed.

2. **Increase item size and spacing** (RoundMenu.tsx + RoundMenu.module.css):
   - Change button size from 'small' to 'medium' (or remove the size='small' hardcode at line 49 to use default size).
   - Scale the radius calculation to be larger -- check the Vue source formula for radius scaling with item count. Increase the base radius or multiplier so items are more spread out.
   - Ensure the trigger button itself also uses a reasonable default size (not 'small').
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "left.*0\|top.*0\|transform-origin\|size=" src/components/RoundMenu/RoundMenu.tsx src/components/RoundMenu/RoundMenu.module.css | head -10</automated>
  </verify>
  <done>RoundMenu items positioned from stable anchor. Label expands rightward. Items properly sized and spaced.</done>
</task>

</tasks>

<verification>
- Stepper story: past steps show original icons (not checkmarks), future steps lighter
- Stepper: clicking non-adjacent step with allowSkip=false does nothing
- RoundMenu story: click trigger, items appear properly spaced. Show label -- button expands right, items don't shift
</verification>

<success_criteria>
- Stepper visual matches: green past with original icons, active with ring, lighter future
- allowSkip enforcement works
- RoundMenu stable positioning with proper sizing
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-26-SUMMARY.md`
</output>
