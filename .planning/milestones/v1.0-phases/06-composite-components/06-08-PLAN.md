---
phase: 06-composite-components
plan: 08
type: execute
wave: 2
depends_on: ["06-04", "06-07"]
files_modified:
  - src/components/DatePicker/DatePicker.tsx
  - src/components/DatePicker/DatePicker.module.css
  - src/components/DatePicker/DatePicker.test.tsx
  - src/components/Navbar/Navbar.tsx
  - src/components/Navbar/Navbar.module.css
  - src/components/Navbar/Navbar.test.tsx
autonomous: true
requirements:
  - COMP-13
  - COMP-16

must_haves:
  truths:
    - "DatePicker wraps Calendar in ExpandableContainer"
    - "DatePicker action buttons use string label props (clearLabel, applyLabel, compareLabel)"
    - "Navbar renders navigation items with internal Dropdown"
    - "Navbar uses Dropdown component (not reimplemented)"
  artifacts:
    - path: "src/components/DatePicker/DatePicker.tsx"
      provides: "DatePicker wrapping Calendar in ExpandableContainer"
      exports: ["DatePicker", "DatePickerProps"]
    - path: "src/components/Navbar/Navbar.tsx"
      provides: "Navigation bar with Dropdown"
      exports: ["Navbar", "NavbarProps"]
  key_links:
    - from: "src/components/DatePicker/DatePicker.tsx"
      to: "src/components/Calendar/Calendar.tsx"
      via: "import { Calendar }"
      pattern: "import.*Calendar"
    - from: "src/components/DatePicker/DatePicker.tsx"
      to: "src/utils/components/ExpandableContainer.tsx"
      via: "import { ExpandableContainer }"
      pattern: "import.*ExpandableContainer"
    - from: "src/components/Navbar/Navbar.tsx"
      to: "src/components/Dropdown/Dropdown.tsx"
      via: "import { Dropdown }"
      pattern: "import.*Dropdown"
---

<objective>
Migrate DatePicker and Navbar — two Wave 2 components that depend on Calendar and Dropdown respectively.

Purpose: DatePicker wraps Calendar in ExpandableContainer. Navbar uses Dropdown for navigation items. Both require their dependencies to be migrated first.
Output: Working DatePicker and Navbar with tests.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-composite-components/06-CONTEXT.md
@.planning/phases/06-composite-components/06-RESEARCH.md
@.planning/phases/06-composite-components/06-UI-SPEC.md
@.planning/phases/06-composite-components/06-04-SUMMARY.md
@.planning/phases/06-composite-components/06-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DatePicker</name>
  <files>src/components/DatePicker/DatePicker.tsx, src/components/DatePicker/DatePicker.module.css, src/components/DatePicker/DatePicker.test.tsx</files>
  <read_first>
    src/components/DatePicker/DatePicker.vue,
    src/components/DatePicker/DatePicker.tsx,
    src/components/Calendar/Calendar.tsx,
    src/utils/components/ExpandableContainer.tsx,
    src/components/Button/Button.tsx,
    src/utils/index.ts
  </read_first>
  <action>
Port DatePicker.vue (288 lines) to React TSX. DatePicker wraps Calendar inside ExpandableContainer.

Props interface `DatePickerProps`:
- `value?: Date | Date[] | [Date[], Date[]]` — selected date(s)
- `defaultValue?: Date | Date[] | [Date[], Date[]]` — initial value
- `onChange?: (value: any) => void` — date change callback
- `type?: 'date' | 'period' | 'compare'` — calendar mode, default `'date'`
- `minDate?: Date` — minimum selectable date
- `maxDate?: Date` — maximum selectable date
- `disabledDates?: Date[]` — disabled dates
- `disabled?: boolean` — default `false`
- `labelValue?: string` — trigger label, default `''`
- `isError?: boolean` — default `false`
- `errorMessage?: string` — default `''`
- `infoMessage?: string` — default `''`
- `required?: boolean` — default `false`
- `clearLabel?: string` — clear button text, default `'Clear selection'`
- `applyLabel?: string` — apply button text, default `'Apply filters'`
- `compareLabel?: string` — compare toggle text, default `'Compare dates'`
- `lang?: string` — locale, default `'en'`
- `onApply?: (value: any) => void` — apply callback
- `className?: string`

Implementation:
- `useControllable` for value
- Internal `useState` for expanded (passed to ExpandableContainer)
- Internal `useState` for working date (copy of value while picker is open)
- Display label: format selected date(s) for trigger text using `dateOptions` from utils
- ExpandableContainer wraps everything. `card` prop contains:
  - Calendar component with current working date and type
  - If type is `'compare'`, render compareLabel toggle
  - Actions row: Clear Button (neutral variant, `clearLabel`) + Apply Button (primary variant, `applyLabel`)
- Clear: reset working date, call onChange with null/empty
- Apply: set value to working date, collapse, call onApply

**DatePicker.module.css:**
```css
@reference "../../assets/main.css";

.datePickerCard { @apply p-sm; }
.actions { @apply flex justify-end gap-xs mt-sm pt-xs border-t border-neutral-border-default; }
.compareToggle { @apply flex items-center gap-xs mt-xs; }
.displayLabel { @apply text-sm font-normal truncate; }
```

**Update DatePicker.test.tsx:**
- Renders with ExpandableContainer trigger
- Shows Calendar inside expanded card
- Clear button resets value
- Apply button calls onApply
- Display label shows formatted date
- Renders with custom clearLabel and applyLabel strings
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npx vitest run src/components/DatePicker/DatePicker.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DatePicker.tsx contains `export function DatePicker` and `export interface DatePickerProps`
    - DatePicker.tsx contains `import { Calendar }` from Calendar
    - DatePicker.tsx contains `import { ExpandableContainer }` from utils
    - DatePicker.tsx contains `import { Button }` from Button
    - DatePicker.tsx contains `clearLabel`, `applyLabel`, `compareLabel` string props
    - DatePicker.tsx contains `useControllable`
    - DatePicker.test.tsx passes all tests with exit code 0
  </acceptance_criteria>
  <done>DatePicker wraps Calendar in ExpandableContainer with clear/apply/compare actions.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Navbar</name>
  <files>src/components/Navbar/Navbar.tsx, src/components/Navbar/Navbar.module.css, src/components/Navbar/Navbar.test.tsx</files>
  <read_first>
    src/components/Navbar/Navbar.vue,
    src/components/Navbar/Navbar.tsx,
    src/components/Dropdown/Dropdown.tsx,
    src/components/Icon/Icon.tsx
  </read_first>
  <action>
Port Navbar.vue (139 lines) to React TSX. Navbar renders a navigation bar that uses Dropdown for items with sub-navigation.

Props interface `NavbarProps`:
- `options: Array<{ label: string; icon?: string; onClick?: () => void; options?: any[]; [key: string]: any }>` — nav items
- `labelKey?: string` — default `'label'`
- `iconKey?: string` — default `'icon'`
- `logo?: React.ReactNode` — logo content (left side)
- `profile?: React.ReactNode` — profile content (right side)
- `title?: string` — navbar title
- `className?: string`

Implementation:
- Container: `bg-neutral-surface-default`, `border-b-xxs border-neutral-default`, horizontal flex
- Logo slot: left-anchored, renders `logo` prop
- Title: `text-xl font-semibold`
- Navigation items: rendered in a horizontal flex with `gap-base`
- For each option:
  - If option has `options` array: render as Dropdown with sub-navigation
  - If no sub-options: render as clickable item (`text-sm font-normal leading-lg text-primary-foreground-high`)
  - Icon: render Icon component if `option[iconKey]` exists
- Profile slot: right-anchored, renders `profile` prop

**Navbar.module.css:**
```css
@reference "../../assets/main.css";

.navbar {
  @apply flex items-center justify-between w-full;
  @apply bg-neutral-surface-default;
  border-bottom: theme('borderWidth.xxs') solid theme('colors.neutral-border-default');
  @apply px-base py-xs;
}

.logo { @apply flex items-center; }
.title { @apply text-xl font-semibold; }

.navItems { @apply flex items-center gap-base; }

.navItem {
  @apply text-sm font-normal leading-lg text-primary-foreground-high cursor-pointer;
  @apply flex items-center gap-xxs;
}
.navItem:hover { @apply text-primary-interaction-hover; }

.profile { @apply flex items-center; }
```

**Update Navbar.test.tsx:**
- Renders with navigation options
- Renders logo when provided
- Renders profile when provided
- Renders Dropdown for options with sub-options
- Renders plain items for options without sub-options
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npx vitest run src/components/Navbar/Navbar.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Navbar.tsx contains `export function Navbar` and `export interface NavbarProps`
    - Navbar.tsx contains `import { Dropdown }` from Dropdown
    - Navbar.module.css contains `.navbar` with `bg-neutral-surface-default`
    - Navbar.module.css contains `border-bottom` with `border-neutral-border-default`
    - Navbar.test.tsx passes all tests with exit code 0
  </acceptance_criteria>
  <done>Navbar renders navigation bar with Dropdown for sub-navigation items.</done>
</task>

</tasks>

<verification>
`npx vitest run src/components/DatePicker/DatePicker.test.tsx src/components/Navbar/Navbar.test.tsx` — all pass
</verification>

<success_criteria>
- DatePicker wraps Calendar in ExpandableContainer
- DatePicker uses string label props for actions (not compound sub-components)
- Navbar uses Dropdown internally for sub-navigation
- Both pass tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-08-SUMMARY.md`
</output>
