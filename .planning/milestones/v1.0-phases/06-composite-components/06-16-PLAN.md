---
phase: 06-composite-components
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/main.css
  - src/components/Icon/Icon.css
autonomous: true
requirements: [COMP-05, COMP-06, COMP-11, COMP-13, COMP-14, COMP-16]
gap_closure: true

must_haves:
  truths:
    - "Material Symbols Rounded font loads in the browser"
    - "Icon component renders actual glyphs instead of text strings"
    - "Pagination arrows display as arrow icons"
  artifacts:
    - path: "src/assets/main.css"
      provides: "Google Fonts @import for Material Symbols Rounded"
      contains: "Material+Symbols+Rounded"
    - path: "src/components/Icon/Icon.css"
      provides: "Explicit font-family declaration for icon class"
      contains: "font-family"
  key_links:
    - from: "src/assets/main.css"
      to: "Google Fonts CDN"
      via: "@import url"
      pattern: "@import url.*Material.Symbols.Rounded"
    - from: "src/components/Icon/Icon.css"
      to: ".material-symbols-rounded"
      via: "font-family declaration"
      pattern: "font-family.*Material Symbols Rounded"
---

<objective>
Fix global Material Symbols Rounded font loading so all icon-rendering components display actual icon glyphs instead of text strings like "chevron_left".

Purpose: This is the single highest-impact fix — resolves icon rendering across Pagination, Tab, Stepper, RoundMenu, Calendar, and Navbar (UAT tests 4, 5, 6, 13, 14, 16).
Output: Two CSS files fixed; icons render as glyphs everywhere.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/assets/main.css
@src/components/Icon/Icon.css
@src/components/Icon/Icon.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Material Symbols Rounded font import and fix Icon.css</name>
  <files>src/assets/main.css, src/components/Icon/Icon.css</files>
  <action>
1. In `src/assets/main.css`, add the Material Symbols Rounded Google Fonts import at the top, right after the existing Inter and Poppins imports (line 2):
   ```
   @import url("https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap");
   ```
   Note: Use css2 API (not css) for variable font support with the opsz/wght/FILL/GRAD axes.

2. In `src/components/Icon/Icon.css`, add an explicit `font-family: 'Material Symbols Rounded';` declaration to the `.material-symbols-rounded` rule. This is needed because `main.css` has a `* { font-family: var(--font-sans); }` rule that overrides the font. The explicit declaration ensures the icon font takes precedence.

   Updated `.material-symbols-rounded` rule:
   ```css
   .material-symbols-rounded {
     font-family: 'Material Symbols Rounded';
     user-select: none;
     font-variation-settings:
       "FILL" 0,
       "wght" 400,
       "GRAD" 0,
       "opsz" 24;
   }
   ```
   Keep the `.material-symbols-rounded.filled` rule unchanged.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -q "Material.Symbols.Rounded" src/assets/main.css && grep -q "font-family" src/components/Icon/Icon.css && echo "PASS: Font import and font-family present"</automated>
  </verify>
  <done>main.css imports Material Symbols Rounded font; Icon.css has explicit font-family declaration; all Icon usages across the design system will render actual glyphs.</done>
</task>

</tasks>

<verification>
- `grep "Material+Symbols+Rounded" src/assets/main.css` returns the @import line
- `grep "font-family" src/components/Icon/Icon.css` returns the font-family declaration
- Storybook Icon story (`npm run storybook`) shows rendered icon glyphs, not text strings
</verification>

<success_criteria>
- Material Symbols Rounded @import is present in main.css
- Icon.css .material-symbols-rounded rule has font-family: 'Material Symbols Rounded'
- Icons render as glyphs (not text) in all components that use the Icon component or material-symbols-rounded class
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-16-SUMMARY.md`
</output>
