---
phase: 06-composite-components
plan: 18
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Dropdown/Dropdown.tsx
  - src/components/TagSelect/TagSelect.tsx
autonomous: true
requirements: [COMP-09, COMP-10]
gap_closure: true

must_haves:
  truths:
    - "Dropdown accepts required, infoMessage, maxHeight, minWidth, absolute props"
    - "TagSelect accepts icon, expanded, absolute, buttonLabel props"
  artifacts:
    - path: "src/components/Dropdown/Dropdown.tsx"
      provides: "5 missing props added and forwarded to ExpandableContainer"
    - path: "src/components/TagSelect/TagSelect.tsx"
      provides: "4 missing props added and wired"
  key_links:
    - from: "src/components/Dropdown/Dropdown.tsx"
      to: "src/utils/components/ExpandableContainer.tsx"
      via: "prop forwarding"
      pattern: "required.*infoMessage.*maxHeight.*minWidth.*absolute"
---

<objective>
Add missing props to Dropdown (5 props) and TagSelect (4 props) that the Vue originals had but the React ports omitted (UAT tests 9, 10).

Purpose: Simple prop pass-through fixes — ExpandableContainer already supports these props, they just need declaration and forwarding.
Output: Dropdown and TagSelect with full original prop APIs.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Dropdown/Dropdown.tsx
@src/components/TagSelect/TagSelect.tsx
@src/utils/components/ExpandableContainer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 missing props to Dropdown</name>
  <files>src/components/Dropdown/Dropdown.tsx</files>
  <action>
1. Add 5 props to `DropdownProps` interface:
   ```typescript
   required?: boolean;
   infoMessage?: string;
   maxHeight?: string;
   minWidth?: string;
   absolute?: boolean;
   ```

2. Destructure these 5 new props in the Dropdown component function with defaults:
   ```typescript
   required = false,
   infoMessage,
   maxHeight,
   minWidth,
   absolute = true,
   ```

3. Forward all 5 props to the `ExpandableContainer` invocation. Find where `<ExpandableContainer` is rendered and add:
   ```tsx
   required={required}
   infoMessage={infoMessage}
   maxHeight={maxHeight}
   minWidth={minWidth}
   absolute={absolute}
   ```
   ExpandableContainer already accepts all of these (see its props interface: required, infoMessage, maxHeight, minWidth are forwarded to Container; absolute controls positioning).
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -c "required\|infoMessage\|maxHeight\|minWidth\|absolute" src/components/Dropdown/Dropdown.tsx | awk '{if ($1 >= 10) print "PASS"; else print "FAIL: expected at least 10 occurrences"}'</automated>
  </verify>
  <done>Dropdown accepts and forwards required, infoMessage, maxHeight, minWidth, absolute to ExpandableContainer.</done>
</task>

<task type="auto">
  <name>Task 2: Add 4 missing props to TagSelect</name>
  <files>src/components/TagSelect/TagSelect.tsx</files>
  <action>
1. Add 4 props to `TagSelectProps` interface:
   ```typescript
   icon?: string;
   expanded?: boolean;
   onExpandedChange?: (expanded: boolean) => void;
   absolute?: boolean;
   buttonLabel?: string;
   ```

2. Destructure these props in the TagSelect component:
   ```typescript
   icon,
   expanded,
   onExpandedChange,
   absolute = true,
   buttonLabel,
   ```

3. Wire `expanded` / `onExpandedChange` as controlled state for the dropdown open/close. TagSelect likely uses ExpandableContainer or has internal open state. If it uses ExpandableContainer, forward `expanded` as `value` and `onExpandedChange` as part of `onChange`. If it has internal state via `useState`, convert to `useControllable` pattern:
   ```typescript
   const [isOpen, setIsOpen] = useControllable<boolean>({
     value: expanded,
     defaultValue: false,
     onChange: onExpandedChange,
   });
   ```

4. Wire `icon` prop: If TagSelect renders a trigger button or SelectContent, pass `icon` to it. Look for how SelectContent or the trigger area is rendered and add the icon prop.

5. Wire `absolute` prop: Forward to ExpandableContainer if used, or apply to the dropdown content div's positioning.

6. Wire `buttonLabel` prop: Replace any hardcoded trigger button label with `buttonLabel` when provided.

7. Add keyboard navigation basics: In the dropdown content area, add `onKeyDown` handler:
   - ArrowDown: move focus to next option
   - ArrowUp: move focus to previous option
   - Home: move to first option
   - End: move to last option
   - Tab: if in creatable mode and there's text in the search input, create the tag
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -q "icon" src/components/TagSelect/TagSelect.tsx && grep -q "expanded" src/components/TagSelect/TagSelect.tsx && grep -q "absolute" src/components/TagSelect/TagSelect.tsx && grep -q "buttonLabel" src/components/TagSelect/TagSelect.tsx && echo "PASS"</automated>
  </verify>
  <done>TagSelect accepts icon, expanded/onExpandedChange, absolute, buttonLabel props; keyboard navigation supported.</done>
</task>

</tasks>

<verification>
- Dropdown stories with required and infoMessage props render correctly
- TagSelect with icon prop shows icon in trigger; expanded prop controls open state
</verification>

<success_criteria>
- Dropdown has all 5 missing props declared, destructured, and forwarded to ExpandableContainer
- TagSelect has all 4 missing props declared, destructured, and wired to functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-18-SUMMARY.md`
</output>
