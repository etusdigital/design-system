---
phase: 08-build-distribution
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar/Sidebar.mdx
  - src/components/Skeleton/Skeleton.mdx
  - src/components/Slider/Slider.mdx
  - src/components/Spinner/Spinner.mdx
  - src/components/StatusBadge/StatusBadge.mdx
  - src/components/Stepper/Stepper.mdx
  - src/components/Switch/Switch.mdx
  - src/components/Tab/Tab.mdx
  - src/components/Table/Table.mdx
  - src/components/TagInput/TagInput.mdx
  - src/components/TagSelect/TagSelect.mdx
  - src/components/Textarea/Textarea.mdx
  - src/components/Toast/Toast.mdx
  - src/components/Toggle/Toggle.mdx
  - src/components/ToggleGroup/ToggleGroup.mdx
  - src/components/Tooltip/Tooltip.mdx
  - src/components/Tree/Tree.mdx
  - src/documentation/Introduction.mdx
  - src/documentation/Themes.mdx
  - src/documentation/Version.mdx
autonomous: true
requirements:
  - DIST-04

must_haves:
  truths:
    - "All 20 MDX files in this batch contain tsx code fences instead of vue code fences"
    - "No MDX file in this batch contains <template> or <script setup> tags"
    - "Documentation MDX files (Introduction, Themes, Version) reference React, not Vue"
  artifacts:
    - path: "src/components/Table/Table.mdx"
      provides: "React JSX documentation for Table component"
      contains: "```tsx"
    - path: "src/documentation/Introduction.mdx"
      provides: "Updated introduction referencing React design system"
  key_links:
    - from: "src/components/*/ComponentName.mdx"
      to: "src/components/*/ComponentName.stories.tsx"
      via: "MDX import statement"
      pattern: "import \\* as .+ from './.+\\.stories\\.tsx'"
---

<objective>
Update the final batch of 17 component MDX files plus 3 documentation MDX files from Vue syntax to React JSX syntax.

Purpose: Per CONTEXT.md locked decision, all .mdx files must reflect React usage. This is the final batch covering Sidebar through Tree alphabetically, plus the 3 documentation pages (Introduction, Themes, Version).

Output: 20 MDX files converted from Vue to React documentation syntax. All 60 MDX files across Plans 03-05 are now React-based.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-build-distribution/08-CONTEXT.md
@.planning/phases/08-build-distribution/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert MDX files batch 3 (Sidebar through Tree + documentation pages) from Vue to React syntax</name>
  <files>
    src/components/Sidebar/Sidebar.mdx,
    src/components/Skeleton/Skeleton.mdx,
    src/components/Slider/Slider.mdx,
    src/components/Spinner/Spinner.mdx,
    src/components/StatusBadge/StatusBadge.mdx,
    src/components/Stepper/Stepper.mdx,
    src/components/Switch/Switch.mdx,
    src/components/Tab/Tab.mdx,
    src/components/Table/Table.mdx,
    src/components/TagInput/TagInput.mdx,
    src/components/TagSelect/TagSelect.mdx,
    src/components/Textarea/Textarea.mdx,
    src/components/Toast/Toast.mdx,
    src/components/Toggle/Toggle.mdx,
    src/components/ToggleGroup/ToggleGroup.mdx,
    src/components/Tooltip/Tooltip.mdx,
    src/components/Tree/Tree.mdx,
    src/documentation/Introduction.mdx,
    src/documentation/Themes.mdx,
    src/documentation/Version.mdx
  </files>
  <read_first>
    src/components/Table/Table.mdx,
    src/components/Tree/Tree.mdx,
    src/documentation/Introduction.mdx
  </read_first>
  <action>
    For EACH of the 20 MDX files listed above, read the file and apply the same transform rules as Plans 03-04:

    **Component MDX transform rules (17 files):**

    1. **Code fence language**: Replace all ` ```vue ` with ` ```tsx `
    2. **Remove template wrappers**: Delete `<template>` and `</template>` lines
    3. **Remove script blocks**: Delete `<script setup>` ... `</script>` blocks
    4. **Event syntax**: `@eventName="handler"` -> `onEventName={handler}`
    5. **v-bind shorthand**: `:prop="value"` -> `prop={value}`
    6. **v-model**: `v-model="val"` -> `value={val} onChange={setVal}`
    7. **v-if / v-show**: -> conditional rendering with `{condition && ...}`
    8. **v-for**: -> `.map()` pattern
    9. **Slot syntax**: `#slotName` -> React children or named props
    10. **Import statement**: Ensure references `.stories.tsx`
    11. **Vue-specific text**: "Vue component" -> "React component", "slots" -> "children/props"

    **Documentation MDX transform rules (3 files: Introduction.mdx, Themes.mdx, Version.mdx):**

    These files may contain:
    - References to "Vue" framework -> change to "React"
    - Vue-specific installation instructions -> update to React
    - `import { createApp } from 'vue'` examples -> `import { DesignSystemProvider } from '@etus/design-system'`
    - Vue plugin install pattern (`app.use(DesignSystem)`) -> React provider pattern (`<DesignSystemProvider>`)
    - References to `v-model`, `$confirm`, `$toast` -> `value/onChange`, `useConfirm()`, `useToast()`
    - CSS import instructions should show: `import '@etus/design-system/styles.css'`

    For Version.mdx specifically, update to note version 2.0.0 as the React migration release.

    **IMPORTANT**: Keep all Storybook `<Canvas>`, `<Meta>`, `<Controls>` blocks unchanged.

    **Special attention for complex components:**
    - Table.mdx: render prop columns use `render: (row) => <span>{row.name}</span>` not Vue scoped slots
    - Tree.mdx: compound component pattern `<Tree><Tree.Node>` not Vue slot pattern
    - Toast.mdx: `useToast()` hook not `$toast` global

    Process all 20 files.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && for f in Sidebar Skeleton Slider Spinner StatusBadge Stepper Switch Tab Table TagInput TagSelect Textarea Toast Toggle ToggleGroup Tooltip Tree; do if grep -q '```vue' "src/components/$f/$f.mdx" 2>/dev/null; then echo "FAIL: $f.mdx still has vue code fence"; fi; done && for f in Introduction Themes Version; do if grep -q '```vue' "src/documentation/$f.mdx" 2>/dev/null; then echo "FAIL: $f.mdx still has vue code fence"; fi; done && echo "BATCH 3 CHECK COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - None of the 17 component MDX files contain "```vue"
    - None of the 17 component MDX files contain "<template>" or "</template>"
    - None of the 17 component MDX files contain "<script setup>"
    - None of the 3 documentation MDX files contain "```vue"
    - Introduction.mdx references React (not Vue) for framework
    - All code examples use ```tsx code fences
    - All event handlers use React syntax
    - Import statements reference .stories.tsx files
  </acceptance_criteria>
  <done>Final 20 MDX files (Sidebar through Tree + 3 documentation pages) converted. All 60 MDX files across the project now use React syntax.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 60 MDX files are Vue-free</name>
  <files>None (verification only)</files>
  <read_first>None</read_first>
  <action>
    Run a comprehensive check across ALL 60 MDX files to confirm zero Vue syntax remains:

    ```bash
    # Check for any remaining Vue code fences
    grep -rl '```vue' src/ --include="*.mdx"

    # Check for template tags
    grep -rl '<template>' src/ --include="*.mdx"

    # Check for script setup
    grep -rl '<script setup>' src/ --include="*.mdx"

    # Check for Vue event syntax
    grep -rn '@click=\|@change=\|@input=\|@update:' src/ --include="*.mdx"

    # Check for v-model
    grep -rn 'v-model=' src/ --include="*.mdx"

    # Check for v-if/v-for/v-show
    grep -rn 'v-if=\|v-for=\|v-show=' src/ --include="*.mdx"
    ```

    All commands should return empty (no matches). If any matches are found, fix them in the corresponding MDX file.

    Also verify the total count of MDX files is still 60:
    ```bash
    find src/ -name "*.mdx" | wc -l
    ```
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && FAILURES=0; if grep -rl '```vue' src/ --include="*.mdx" 2>/dev/null | head -1 | grep -q .; then echo "FAIL: vue code fences found"; FAILURES=$((FAILURES+1)); fi; if grep -rl '<template>' src/ --include="*.mdx" 2>/dev/null | head -1 | grep -q .; then echo "FAIL: template tags found"; FAILURES=$((FAILURES+1)); fi; if grep -rl 'v-model=' src/ --include="*.mdx" 2>/dev/null | head -1 | grep -q .; then echo "FAIL: v-model found"; FAILURES=$((FAILURES+1)); fi; if [ $FAILURES -eq 0 ]; then echo "ALL 60 MDX FILES VERIFIED VUE-FREE"; fi</automated>
  </verify>
  <acceptance_criteria>
    - grep for "```vue" across all src/**/*.mdx returns zero matches
    - grep for "<template>" across all src/**/*.mdx returns zero matches
    - grep for "v-model=" across all src/**/*.mdx returns zero matches
    - grep for "@click=" across all src/**/*.mdx returns zero matches
    - Total MDX file count is 60
  </acceptance_criteria>
  <done>All 60 MDX files across the entire project confirmed Vue-free. Documentation fully reflects React usage.</done>
</task>

</tasks>

<verification>
```bash
# Final comprehensive MDX verification
grep -rl '```vue\|<template>\|<script setup>\|v-model=\|@click=' src/ --include="*.mdx" | wc -l
# Must output: 0
```
</verification>

<success_criteria>
1. All 17 component MDX files in this batch use ```tsx code fences
2. All 3 documentation MDX files updated for React
3. Zero Vue syntax across all 60 MDX files in the project
4. Introduction.mdx references React as the framework
</success_criteria>

<output>
After completion, create `.planning/phases/08-build-distribution/08-05-SUMMARY.md`
</output>
