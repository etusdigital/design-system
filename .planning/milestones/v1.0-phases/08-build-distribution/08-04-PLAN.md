---
phase: 08-build-distribution
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FileUpload/FileUpload.mdx
  - src/components/Filter/Filter.mdx
  - src/components/FloatCard/FloatCard.mdx
  - src/components/History/History.mdx
  - src/components/Icon/Icon.mdx
  - src/components/IconCard/IconCard.mdx
  - src/components/Image/Image.mdx
  - src/components/Input/Input.mdx
  - src/components/MetricCard/MetricCard.mdx
  - src/components/Navbar/Navbar.mdx
  - src/components/PINInput/PINInput.mdx
  - src/components/Pagination/Pagination.mdx
  - src/components/Profile/Profile.mdx
  - src/components/ProgressBar/ProgressBar.mdx
  - src/components/Radio/Radio.mdx
  - src/components/RadioGroup/RadioGroup.mdx
  - src/components/RichTextEditor/RichTextEditor.mdx
  - src/components/RoundMenu/RoundMenu.mdx
  - src/components/Select/Select.mdx
  - src/components/Separator/Separator.mdx
autonomous: true
requirements:
  - DIST-04

must_haves:
  truths:
    - "All 20 MDX files in this batch contain tsx code fences instead of vue code fences"
    - "No MDX file in this batch contains <template> or <script setup> tags"
    - "No MDX file in this batch contains Vue event syntax (@click, @change, etc.)"
  artifacts:
    - path: "src/components/Input/Input.mdx"
      provides: "React JSX documentation for Input component"
      contains: "```tsx"
    - path: "src/components/Select/Select.mdx"
      provides: "React JSX documentation for Select component"
      contains: "```tsx"
  key_links:
    - from: "src/components/*/ComponentName.mdx"
      to: "src/components/*/ComponentName.stories.tsx"
      via: "MDX import statement"
      pattern: "import \\* as .+ from './.+\\.stories\\.tsx'"
---

<objective>
Update the second batch of 20 component MDX documentation files from Vue syntax to React JSX syntax.

Purpose: Per CONTEXT.md locked decision, all .mdx files must reflect React usage. This is the second batch covering FileUpload through Separator alphabetically.

Output: 20 MDX files converted from Vue to React documentation syntax.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-build-distribution/08-CONTEXT.md
@.planning/phases/08-build-distribution/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert MDX files batch 2 (FileUpload through Separator) from Vue to React syntax</name>
  <files>
    src/components/FileUpload/FileUpload.mdx,
    src/components/Filter/Filter.mdx,
    src/components/FloatCard/FloatCard.mdx,
    src/components/History/History.mdx,
    src/components/Icon/Icon.mdx,
    src/components/IconCard/IconCard.mdx,
    src/components/Image/Image.mdx,
    src/components/Input/Input.mdx,
    src/components/MetricCard/MetricCard.mdx,
    src/components/Navbar/Navbar.mdx,
    src/components/PINInput/PINInput.mdx,
    src/components/Pagination/Pagination.mdx,
    src/components/Profile/Profile.mdx,
    src/components/ProgressBar/ProgressBar.mdx,
    src/components/Radio/Radio.mdx,
    src/components/RadioGroup/RadioGroup.mdx,
    src/components/RichTextEditor/RichTextEditor.mdx,
    src/components/RoundMenu/RoundMenu.mdx,
    src/components/Select/Select.mdx,
    src/components/Separator/Separator.mdx
  </files>
  <read_first>
    src/components/Input/Input.mdx,
    src/components/Select/Select.mdx,
    src/components/RadioGroup/RadioGroup.mdx
  </read_first>
  <action>
    For EACH of the 20 MDX files listed above, read the file and apply the same transform rules as Plan 03:

    **Transform rules (apply in order):**

    1. **Code fence language**: Replace all ` ```vue ` with ` ```tsx `
    2. **Remove template wrappers**: Delete `<template>` and `</template>` lines entirely
    3. **Remove script blocks**: Delete `<script setup>` ... `</script>` blocks. Replace with React useState if the example needs state
    4. **Event syntax**: `@eventName="handler"` -> `onEventName={handler}`
    5. **v-bind shorthand**: `:prop="value"` -> `prop={value}`
    6. **v-model**: `v-model="val"` -> `value={val} onChange={setVal}`
    7. **v-if / v-show**: `v-if="condition"` -> `{condition && <Component />}`
    8. **v-for**: `v-for="item in items"` -> `{items.map(item => ...)}`
    9. **Slot syntax**: `#slotName` -> React children or named props
    10. **Import statement**: Ensure references `.stories.tsx`
    11. **Vue-specific text**: Replace "Vue component" -> "React component", "slots" -> "children/props", "emit" -> "callback prop"

    **IMPORTANT**: Keep all Storybook `<Canvas>`, `<Meta>`, `<Controls>` blocks unchanged.

    **IMPORTANT**: For form component MDX files (Input, Radio, RadioGroup, PINInput, FileUpload, Select), pay special attention to:
    - `v-model` -> `value` + `onChange` props
    - `ref()` state -> `useState()` hook
    - `defineProps` -> just show the props directly on the component

    Process all 20 files. Read each file, apply transforms, write the updated file.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && for f in FileUpload Filter FloatCard History Icon IconCard Image Input MetricCard Navbar PINInput Pagination Profile ProgressBar Radio RadioGroup RichTextEditor RoundMenu Select Separator; do if grep -q '```vue' "src/components/$f/$f.mdx" 2>/dev/null; then echo "FAIL: $f.mdx still has vue code fence"; fi; done && echo "BATCH 2 CHECK COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - None of the 20 MDX files contain the string "```vue"
    - None of the 20 MDX files contain "<template>" or "</template>"
    - None of the 20 MDX files contain "<script setup>"
    - All code examples use ```tsx code fences
    - All event handlers use React syntax (onClick, onChange) not Vue syntax (@click, @change)
    - Import statements reference .stories.tsx files
    - Storybook <Canvas>, <Meta>, <Controls> blocks are preserved unchanged
  </acceptance_criteria>
  <done>20 MDX files (FileUpload through Separator) converted from Vue to React documentation syntax with no Vue-specific syntax remaining.</done>
</task>

</tasks>

<verification>
```bash
for f in FileUpload Filter FloatCard History Icon IconCard Image Input MetricCard Navbar PINInput Pagination Profile ProgressBar Radio RadioGroup RichTextEditor RoundMenu Select Separator; do
  grep -l '```vue\|<template>\|<script setup>\|@click=\|v-model=' "src/components/$f/$f.mdx" 2>/dev/null
done
# Should produce no output
```
</verification>

<success_criteria>
1. All 20 MDX files use ```tsx code fences exclusively
2. No Vue template, script, or directive syntax remains
3. React event handler syntax used throughout
4. Storybook MDX blocks preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-build-distribution/08-04-SUMMARY.md`
</output>
