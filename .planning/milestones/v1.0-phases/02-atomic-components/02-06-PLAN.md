---
phase: 02-atomic-components
plan: 06
type: execute
wave: 3
depends_on: ["02-00", "02-01", "02-02", "02-04"]
files_modified:
  - src/components/MetricCard/MetricCard.tsx
  - src/components/MetricCard/MetricCard.module.css
  - src/components/MetricCard/MetricCard.stories.tsx
  - src/components/Breadcrumb/Breadcrumb.tsx
  - src/components/Breadcrumb/Breadcrumb.module.css
  - src/components/Breadcrumb/Breadcrumb.stories.tsx
autonomous: true
requirements: [ATOM-12, ATOM-16]

must_haves:
  truths:
    - "MetricCard renders metric value with label, Tooltip info, and Skeleton loading state"
    - "MetricCard compound sub-components render named slot content (header, body, footer)"
    - "Breadcrumb renders navigation path with collapsible overflow via FloatCard"
    - "Breadcrumb supports controlled and uncontrolled modes via useControllable"
  artifacts:
    - path: "src/components/MetricCard/MetricCard.tsx"
      provides: "MetricCard with Tooltip + Skeleton integration"
      exports: ["MetricCard", "MetricCardProps"]
    - path: "src/components/Breadcrumb/Breadcrumb.tsx"
      provides: "Breadcrumb navigation with FloatCard overflow"
      exports: ["Breadcrumb", "BreadcrumbProps"]
  key_links:
    - from: "src/components/MetricCard/MetricCard.tsx"
      to: "src/components/Tooltip/Tooltip.tsx"
      via: "import { Tooltip }"
      pattern: "import.*Tooltip"
    - from: "src/components/MetricCard/MetricCard.tsx"
      to: "src/components/Skeleton/Skeleton.tsx"
      via: "import { Skeleton }"
      pattern: "import.*Skeleton"
    - from: "src/components/MetricCard/MetricCard.tsx"
      to: "src/components/Card/Card.tsx"
      via: "import { Card }"
      pattern: "import.*Card"
    - from: "src/components/Breadcrumb/Breadcrumb.tsx"
      to: "src/components/FloatCard/FloatCard.tsx"
      via: "import { FloatCard }"
      pattern: "import.*FloatCard"
    - from: "src/components/Breadcrumb/Breadcrumb.tsx"
      to: "src/hooks/useControllable.ts"
      via: "import { useControllable }"
      pattern: "useControllable"
---

<objective>
Migrate MetricCard (depends on Card, Skeleton, Tooltip, Icon) and Breadcrumb (depends on FloatCard, Icon, useControllable) to React TSX.

Purpose: MetricCard is the most dependency-heavy card variant, integrating Tooltip for info and Skeleton for loading. Breadcrumb demonstrates the useControllable hook for controlled/uncontrolled navigation state.
Output: 2 working React components with stories, Vue files deleted.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-atomic-components/02-CONTEXT.md
@.planning/phases/02-atomic-components/02-RESEARCH.md

Source Vue files:
@src/components/MetricCard/MetricCard.vue
@src/components/Breadcrumb/Breadcrumb.vue

Existing stories:
@src/components/MetricCard/MetricCard.stories.ts
@src/components/Breadcrumb/Breadcrumb.stories.ts

Dependencies (available from Plans 01-04):
@src/components/Card/Card.tsx
@src/components/Skeleton/Skeleton.tsx
@src/components/Tooltip/Tooltip.tsx
@src/components/Icon/Icon.tsx
@src/components/FloatCard/FloatCard.tsx
@src/hooks/useControllable.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate MetricCard to React TSX</name>
  <files>
    src/components/MetricCard/MetricCard.tsx
    src/components/MetricCard/MetricCard.module.css
    src/components/MetricCard/MetricCard.stories.tsx
  </files>
  <action>
Read `MetricCard.vue` — extract all props, named slots, template structure, computed values, CSS.

Create `MetricCard.tsx`:
1. **Props interface:** Extract from Vue source — likely includes `title`, `value`, `info` (tooltip text), `loading`, `icon`, `color`, `children`, `className`, plus others.
2. **Multiple named slots → compound sub-components** per locked decision. Check Vue source for slot names — create `MetricCard.Header`, `MetricCard.Body`, `MetricCard.Footer`, `MetricCard.Actions`, or whatever the Vue source defines.
3. **Tooltip integration:** When `info` prop is set, wrap info icon in `<Tooltip labelValue={info}>`.
4. **Skeleton integration:** When `loading=true`, render `<Skeleton>` placeholders instead of actual content.
5. **Card wrapper:** Import and render within `<Card>`.
6. **Icon:** Import and render `<Icon>` for the metric icon.

Create `MetricCard.module.css` — extract styles from Vue scoped CSS.
Create `MetricCard.stories.tsx` — adapt Vue story with all variants (loading, with info, with icon, etc.).
Delete `MetricCard.vue` and `MetricCard.stories.ts`.
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/MetricCard/MetricCard.test.tsx</automated>
  </verify>
  <done>MetricCard renders with metric value, Tooltip info, Skeleton loading, Icon, and all compound sub-components. Vue files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Breadcrumb to React TSX</name>
  <files>
    src/components/Breadcrumb/Breadcrumb.tsx
    src/components/Breadcrumb/Breadcrumb.module.css
    src/components/Breadcrumb/Breadcrumb.stories.tsx
  </files>
  <action>
Read `Breadcrumb.vue` — extract props, useOptionalModel usage, parsedOptions logic, FloatCard integration, CSS.

Create `Breadcrumb.tsx`:
1. **Props interface:** `options` (array of breadcrumb items), `value`/`onChange` (controlled), `defaultValue` (uncontrolled), `children`, `className`, plus others from Vue source.
2. **useControllable** (Pattern 7 from RESEARCH.md):
   ```tsx
   const [model, setModel] = useControllable({
     value,
     defaultValue: undefined,
     onChange,
   });
   ```
   This replaces Vue's `useOptionalModel`.
3. **parsedOptions logic:** Port the Vue computed property that processes the options array — derive in function body as a regular variable.
4. **Overflow / FloatCard:** When breadcrumb items exceed available space, render collapsed items in a `<FloatCard>` dropdown. Port the exact overflow detection and rendering logic from Vue source.
5. **Icon:** Import `<Icon>` for separator arrows or breadcrumb icons.
6. **Navigation:** Each breadcrumb item is clickable, calling `setModel(item)` on click.

Create `Breadcrumb.module.css` — extract styles from Vue scoped CSS.
Create `Breadcrumb.stories.tsx` — adapt Vue story. Include story with enough items to trigger overflow/FloatCard.
Delete `Breadcrumb.vue` and `Breadcrumb.stories.ts`.
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/Breadcrumb/Breadcrumb.test.tsx</automated>
  </verify>
  <done>Breadcrumb renders navigation path, supports controlled/uncontrolled modes, shows FloatCard overflow for long paths. Vue files deleted.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project unit src/components/MetricCard src/components/Breadcrumb` all pass
- MetricCard and Breadcrumb importable from `src/components`
- No `.vue` or `.stories.ts` (Vue) files remain
- MetricCard shows Skeleton when loading, Tooltip when info provided
- Breadcrumb uses useControllable for value management
</verification>

<success_criteria>
- MetricCard renders all content slots, Tooltip info, Skeleton loading, and Icon
- Breadcrumb renders path, handles overflow with FloatCard, supports controlled/uncontrolled
- Both Storybook stories render without errors
- 4 Vue files deleted
</success_criteria>

<output>
After completion, create `.planning/phases/02-atomic-components/02-06-SUMMARY.md`
</output>
