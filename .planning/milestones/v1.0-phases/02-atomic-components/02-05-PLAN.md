---
phase: 02-atomic-components
plan: 05
type: execute
wave: 3
depends_on: ["02-00", "02-01", "02-02", "02-03"]
files_modified:
  - src/components/ProgressBar/ProgressBar.tsx
  - src/components/ProgressBar/ProgressBar.module.css
  - src/components/ProgressBar/ProgressBar.stories.tsx
  - src/components/ActionCard/ActionCard.tsx
  - src/components/ActionCard/ActionCard.module.css
  - src/components/ActionCard/ActionCard.stories.tsx
  - src/components/IconCard/IconCard.tsx
  - src/components/IconCard/IconCard.module.css
  - src/components/IconCard/colors.module.css
  - src/components/IconCard/IconCard.stories.tsx
autonomous: true
requirements: [ATOM-09, ATOM-14, ATOM-15]

must_haves:
  truths:
    - "ProgressBar renders bar or step modes with correct fill percentage"
    - "ProgressBar wraps icon in Tooltip when infoMessage is provided"
    - "ActionCard renders with drag-and-drop event handling and Card sub-component slot"
    - "ActionCard.Card compound sub-component renders named slot content"
    - "IconCard renders Card with Icon and colored background via inline styles"
    - "IconCard compound sub-components render named slot content"
  artifacts:
    - path: "src/components/ProgressBar/ProgressBar.tsx"
      provides: "ProgressBar component with bar and step modes"
      exports: ["ProgressBar", "ProgressBarProps"]
    - path: "src/components/ActionCard/ActionCard.tsx"
      provides: "ActionCard component with drag events"
      exports: ["ActionCard", "ActionCardProps"]
    - path: "src/components/IconCard/IconCard.tsx"
      provides: "IconCard component with colored icon"
      exports: ["IconCard", "IconCardProps"]
  key_links:
    - from: "src/components/ProgressBar/ProgressBar.tsx"
      to: "src/components/Tooltip/Tooltip.tsx"
      via: "import { Tooltip }"
      pattern: "import.*Tooltip"
    - from: "src/components/ActionCard/ActionCard.tsx"
      to: "src/components/Card/Card.tsx"
      via: "import { Card }"
      pattern: "import.*Card"
    - from: "src/components/ActionCard/ActionCard.tsx"
      to: "src/components/Icon/Icon.tsx"
      via: "import { Icon }"
      pattern: "import.*Icon"
    - from: "src/components/IconCard/IconCard.tsx"
      to: "src/components/Icon/Icon.tsx"
      via: "import { Icon }"
      pattern: "import.*Icon"
    - from: "src/components/IconCard/IconCard.tsx"
      to: "src/components/Card/Card.tsx"
      via: "import { Card }"
      pattern: "import.*Card"
---

<objective>
Migrate ProgressBar (depends on Tooltip), ActionCard (drag events + Card), and IconCard (Card + Icon + colored backgrounds) to React TSX.

Purpose: These three components depend on earlier-wave components (Tooltip, Card, Icon) and demonstrate the dynamic component pattern (ProgressBar), drag event lifecycle (ActionCard), and compound sub-components with color variants (IconCard).
Output: 3 working React components with stories, Vue files deleted.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-atomic-components/02-CONTEXT.md
@.planning/phases/02-atomic-components/02-RESEARCH.md

Source Vue files:
@src/components/ProgressBar/ProgressBar.vue
@src/components/ActionCard/ActionCard.vue
@src/components/IconCard/IconCard.vue

Existing stories:
@src/components/ProgressBar/ProgressBar.stories.ts
@src/components/ActionCard/ActionCard.stories.ts
@src/components/IconCard/IconCard.stories.ts

Dependencies (available from Plans 01-03):
@src/components/Tooltip/Tooltip.tsx
@src/components/Card/Card.tsx
@src/components/Icon/Icon.tsx
@src/components/Spinner/Spinner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ProgressBar to React TSX</name>
  <files>
    src/components/ProgressBar/ProgressBar.tsx
    src/components/ProgressBar/ProgressBar.module.css
    src/components/ProgressBar/ProgressBar.stories.tsx
  </files>
  <action>
Read `ProgressBar.vue` — extract props, template, computed values, dynamic component logic, CSS.

Create `ProgressBar.tsx`:
1. **Props interface:** `progress` (number 0-1), `color`, `size`, `animationType`, `icon`, `infoMessage`, plus any others from Vue source. Add `children`, `className`.
2. **Dual render mode:** Check Vue source for bar vs step rendering logic. Implement both modes based on prop conditions.
3. **Dynamic component → ternary** (Pattern 9 from RESEARCH.md):
   - Vue `<component :is="'Tooltip'">` → React ternary:
   - `{infoMessage ? <Tooltip labelValue={infoMessage} position="bottom">{iconContent}</Tooltip> : <div>{iconContent}</div>}`
4. **Progress width:** Computed as `style={{ width: \`${progress * 100}%\` }}` or similar from Vue source.
5. **Icon slot:** Check for named slots — use compound sub-component if present.
6. **`computedStyleMap()`:** Keep same logic with fallback to `4` (Pitfall from RESEARCH.md — not universally supported, acceptable).

Create `ProgressBar.module.css` — extract styles from Vue scoped CSS. Handle animation keyframes.
Create `ProgressBar.stories.tsx`, delete Vue files.
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/ProgressBar/ProgressBar.test.tsx</automated>
  </verify>
  <done>ProgressBar renders in bar and step modes, wraps icon in Tooltip when infoMessage is set, shows correct fill. Vue files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ActionCard and IconCard to React TSX</name>
  <files>
    src/components/ActionCard/ActionCard.tsx
    src/components/ActionCard/ActionCard.module.css
    src/components/ActionCard/ActionCard.stories.tsx
    src/components/IconCard/IconCard.tsx
    src/components/IconCard/IconCard.module.css
    src/components/IconCard/colors.module.css
    src/components/IconCard/IconCard.stories.tsx
  </files>
  <action>
**ActionCard migration:**
1. Read `ActionCard.vue` — extract props, drag event handling, named slots, CSS.
2. Create `ActionCard.tsx`:
   - Props: `icon`, `color`, `hideDrag`, `children`, `className`, `onDragstart`, `onDragging`, `onDragend`, `onDelete`, plus others from Vue source.
   - **Named slot "card"** → `ActionCard.Card` compound sub-component.
   - **Drag events:** Port touch/mouse drag logic from Vue. FIX the typo: Vue source has `'touhend'` — use correct `'touchend'` (Pitfall 7 from RESEARCH.md).
   - **Window event listeners:** Add `touchmove`, `touchend`, `mousemove`, `mouseup` in drag start handler. Remove all in drag end handler. Use `useEffect` cleanup or `useRef` to track active listeners for cleanup on unmount.
   - Import `{ Card }` and `{ Icon }` as dependencies.
3. Create `ActionCard.module.css`, `ActionCard.stories.tsx`, delete Vue files.

**IconCard migration:**
1. Read `IconCard.vue` — extract props, named slots, color binding, CSS.
2. Create `IconCard.tsx`:
   - Props: `icon`, `color`, `children`, `className`, plus others from Vue source.
   - **Named slots** → compound sub-components (check Vue source for slot names — likely header, body, footer sections).
   - **Color styling:** Vue `v-bind(color)` → `style={{ color, borderColor: color, background: blendColors(color) }}` inline.
   - Import `{ Card }` and `{ Icon }` as dependencies.
3. Create `IconCard.module.css` and `colors.module.css` for color variants. Create `IconCard.stories.tsx`, delete Vue files.
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/ActionCard/ActionCard.test.tsx src/components/IconCard/IconCard.test.tsx</automated>
  </verify>
  <done>ActionCard renders with drag handling, Card sub-component slot, and touchend typo fixed. IconCard renders with colored icon and compound sub-components. Vue files deleted.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project unit src/components/ProgressBar src/components/ActionCard src/components/IconCard` all pass
- ProgressBar, ActionCard, IconCard importable from `src/components`
- No `.vue` or `.stories.ts` (Vue) files remain
- ActionCard drag events use correct `touchend` spelling (not `touhend`)
</verification>

<success_criteria>
- ProgressBar renders both bar and step modes, conditionally wraps icon in Tooltip
- ActionCard handles touch/mouse drag lifecycle, renders Card sub-component
- IconCard renders colored icon with Card wrapper and compound sub-components
- All 3 Storybook stories render without errors
- 6 Vue files deleted
</success_criteria>

<output>
After completion, create `.planning/phases/02-atomic-components/02-05-SUMMARY.md`
</output>
