---
phase: 02-atomic-components
plan: 07
type: execute
wave: 4
depends_on: ["02-00", "02-03", "02-04"]
files_modified:
  - src/components/Image/Image.tsx
  - src/components/Image/Image.module.css
  - src/components/Image/Image.stories.tsx
  - src/components/Profile/Profile.tsx
  - src/components/Profile/Profile.module.css
  - src/components/Profile/Profile.stories.tsx
  - src/components/Icon/Icon.tsx
  - src/components/Icon/Icon.stories.tsx
autonomous: true
requirements: [ATOM-02, ATOM-18, ATOM-20]

must_haves:
  truths:
    - "Image renders with preview modal via createPortal and transition"
    - "Image preview has navigation buttons using Button component"
    - "Image closes preview on Escape key and click-outside"
    - "Profile renders Avatar with dropdown container for user actions"
    - "Profile supports controlled/uncontrolled open state via useControllable"
    - "Icon component (Phase 1) is verified working with all Phase 2 consumers"
  artifacts:
    - path: "src/components/Image/Image.tsx"
      provides: "Image component with preview modal"
      exports: ["Image", "ImageProps"]
    - path: "src/components/Profile/Profile.tsx"
      provides: "Profile dropdown component"
      exports: ["Profile", "ProfileProps"]
  key_links:
    - from: "src/components/Image/Image.tsx"
      to: "src/components/Button/Button.tsx"
      via: "import { Button }"
      pattern: "import.*Button"
    - from: "src/components/Image/Image.tsx"
      to: "react-dom"
      via: "createPortal"
      pattern: "createPortal"
    - from: "src/components/Profile/Profile.tsx"
      to: "src/components/Avatar/Avatar.tsx"
      via: "import { Avatar }"
      pattern: "import.*Avatar"
    - from: "src/components/Profile/Profile.tsx"
      to: "src/components/Button/Button.tsx"
      via: "import { Button }"
      pattern: "import.*Button"
    - from: "src/components/Profile/Profile.tsx"
      to: "src/components/Icon/Icon.tsx"
      via: "import { Icon }"
      pattern: "import.*Icon"
    - from: "src/components/Profile/Profile.tsx"
      to: "src/hooks/useControllable.ts"
      via: "import { useControllable }"
      pattern: "useControllable"
---

<objective>
Migrate Image (preview modal via portal, depends on Button + Icon), Profile (dropdown with Avatar + inline SelectContainer, depends on Button + Avatar + useControllable), and verify Icon integration across all Phase 2 components.

Purpose: Image and Profile are the highest-dependency atomic components. Profile requires an inline implementation of SelectContainer behavior (Phase 4 utility). This plan completes Phase 2.
Output: 2 working React components with stories, Icon verification, Vue files deleted. Phase 2 complete.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-atomic-components/02-CONTEXT.md
@.planning/phases/02-atomic-components/02-RESEARCH.md

Source Vue files:
@src/components/Image/Image.vue
@src/components/Profile/Profile.vue

Existing stories:
@src/components/Image/Image.stories.ts
@src/components/Profile/Profile.stories.ts

Dependencies (available from Plans 01-04):
@src/components/Button/Button.tsx
@src/components/Icon/Icon.tsx
@src/components/Avatar/Avatar.tsx
@src/hooks/useControllable.ts
@src/hooks/useTransition.ts

Internal utility referenced by Profile:
@src/utils/components/SelectContainer.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Image component with preview modal</name>
  <files>
    src/components/Image/Image.tsx
    src/components/Image/Image.module.css
    src/components/Image/Image.stories.tsx
  </files>
  <action>
Read `Image.vue` — extract props, template, preview modal logic, keyboard handling, CSS.

Create `Image.tsx`:
1. **Props interface:** `src` (image URL), `alt`, `width`, `height`, `preview` (boolean to enable preview), `children`, `className`, plus others from Vue source.
2. **Preview modal via portal:**
   - Click on image → open preview modal.
   - `useState(false)` for `isPreviewOpen`.
   - `useTransition(isPreviewOpen, { duration: ... })` for fade.
   - `createPortal(<modal-element>, document.body)` when `isMounted`.
3. **Preview controls:**
   - Import `{ Button }` for close, zoom, navigation buttons.
   - Import `{ Icon }` for button icons.
   - Zoom in/out state for image scaling.
4. **Keyboard handling:**
   - `useEffect` with `document.addEventListener('keydown', handler)`:
     - Escape → close preview.
     - Arrow keys → navigate if multiple images.
   - Cleanup: remove listener in return function.
5. **Click-outside close:** Click on backdrop (outside image) closes preview.
6. **Image element:** Render `<img>` with `src`, `alt`, and styling props.

Create `Image.module.css` — extract styles for image container and preview modal overlay.
Create `Image.stories.tsx` — adapt Vue story. Use `banner.jpg` asset from Image directory.
Delete `Image.vue` and `Image.stories.ts`.
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/Image/Image.test.tsx</automated>
  </verify>
  <done>Image renders, preview modal opens via portal with fade transition, keyboard navigation works, Button controls render. Vue files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Profile component with inline SelectContainer</name>
  <files>
    src/components/Profile/Profile.tsx
    src/components/Profile/Profile.module.css
    src/components/Profile/Profile.stories.tsx
  </files>
  <action>
Read `Profile.vue` — extract props, SelectContainer usage, useOptionalModel usage, slot/template, CSS.
Read `src/utils/components/SelectContainer.vue` — understand the dropdown container behavior that Profile uses.

Create `Profile.tsx`:
1. **Props interface:** `name`, `email`, `src` (avatar image), `options` (dropdown items), `value`/`onChange` (controlled), `defaultValue` (uncontrolled), `children`, `className`, plus others from Vue source. Scoped slot → render prop function for `children` if Profile passes data via children.
2. **useControllable** for open/close or selected value:
   ```tsx
   const [model, setModel] = useControllable({
     value,
     defaultValue,
     onChange,
   });
   ```
3. **Inline SelectContainer behavior** (Pitfall 6 from RESEARCH.md):
   - Do NOT import from `src/utils/components/`.
   - Implement the minimum dropdown behavior locally:
     - `useState(false)` for `isOpen`.
     - Click trigger → toggle open.
     - Click outside → close. Use `useEffect` + `document.addEventListener('mousedown', handler)` with ref check.
     - Render dropdown content below trigger when open.
     - Position via CSS (absolute positioning relative to wrapper).
   - Document clearly with comment: `// Local dropdown implementation — will be replaced by Phase 4 SelectContainer`
4. **Avatar integration:** Import `{ Avatar }` to render user avatar in trigger.
5. **Button integration:** Import `{ Button }` if Profile has action buttons.
6. **Icon integration:** Import `{ Icon }` for dropdown arrow or menu icons.

Create `Profile.module.css` — extract styles, handle dropdown positioning.
Create `Profile.stories.tsx` — adapt Vue story with options array.
Delete `Profile.vue` and `Profile.stories.ts`.
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/Profile/Profile.test.tsx</automated>
  </verify>
  <done>Profile renders Avatar with dropdown, supports controlled/uncontrolled modes, inline SelectContainer works for open/close/click-outside. Vue files deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Icon integration and run full Phase 2 Storybook build</name>
  <files>
    src/components/Icon/Icon.tsx
    src/components/Icon/Icon.stories.tsx
  </files>
  <action>
1. **Verify Icon (ATOM-02):** Icon was migrated in Phase 1. Verify it works correctly with all Phase 2 consumers by running the full Storybook build.
2. **Update Icon.stories.tsx** if needed — check that the existing React story covers the usage patterns from Phase 2 (different sizes, filled/outlined, with className).
3. **Run full Storybook build** to verify all 20 atomic components (19 migrated + Icon verified) render without errors.
4. **Run TypeScript check** across all components.
5. **Verify no remaining Vue files** in any of the 20 atomic component directories:
   - Check: no `.vue` files remain in Spinner, Skeleton, Card, Separator, Avatar, Badge, StatusBadge, Button, Tooltip, FloatCard, Alert, Connector, ProgressBar, ActionCard, IconCard, MetricCard, Breadcrumb, Image, Profile directories.
   - Check: no `.stories.ts` (Vue) files remain (only `.stories.tsx` React files).
6. **Verify all components export** from `src/components/index.ts` → `src/index.ts`.
  </action>
  <verify>
    <automated>npx storybook build --quiet 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All 20 atomic components build in Storybook without errors. Icon verified working across all consumers. No Vue files remain. TypeScript passes. Phase 2 complete.</done>
</task>

</tasks>

<verification>
- `npx storybook build --quiet` completes — all 20 atomic component stories render
- `npx tsc --noEmit` passes with no new errors
- No `.vue` files remain in any of the 20 atomic component directories
- No `.stories.ts` (Vue) files remain — all replaced with `.stories.tsx` (React)
- All 20 components importable from `src/components`
</verification>

<success_criteria>
- Image preview modal works with portal, transition, keyboard, and Button controls
- Profile dropdown works with Avatar, useControllable, and inline SelectContainer
- Icon verified working across all Phase 2 component consumers
- Full Storybook build passes for all 20 atomic components
- Zero Vue files remain in atomic component directories
- Phase 2 is complete: all ATOM-01 through ATOM-20 requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/02-atomic-components/02-07-SUMMARY.md`
</output>
