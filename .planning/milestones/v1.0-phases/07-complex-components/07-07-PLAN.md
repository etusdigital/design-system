---
phase: 07-complex-components
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Table/Table.stories.tsx
  - src/components/RichTextEditor/RichTextEditor.stories.tsx
autonomous: true
requirements: [CPLX-05, CPLX-06]
gap_closure: true

must_haves:
  truths:
    - "Table component viewable in React Storybook with data rendering, sorting, pagination, selection, and aggregation"
    - "RichTextEditor component viewable in React Storybook with toolbar formatting, error state, and disabled state"
  artifacts:
    - path: "src/components/Table/Table.stories.tsx"
      provides: "React Storybook stories for Table"
      contains: "export const Primary"
    - path: "src/components/RichTextEditor/RichTextEditor.stories.tsx"
      provides: "React Storybook stories for RichTextEditor"
      contains: "export const Primary"
  key_links:
    - from: "src/components/Table/Table.stories.tsx"
      to: "src/components/Table/Table.tsx"
      via: "import { Table } from './Table'"
      pattern: "import.*Table.*from.*Table"
    - from: "src/components/RichTextEditor/RichTextEditor.stories.tsx"
      to: "src/components/RichTextEditor/RichTextEditor.tsx"
      via: "import { RichTextEditor } from './RichTextEditor'"
      pattern: "import.*RichTextEditor.*from.*RichTextEditor"
---

<objective>
Create React Storybook stories (.stories.tsx) for Table and RichTextEditor components, mirroring all story variants from the existing Vue .stories.ts files.

Purpose: UAT revealed all 6 Phase 7 components are missing React Storybook stories. This plan covers the 2 most complex components (Table with ~13 stories, RichTextEditor with 6 stories).
Output: 2 new .stories.tsx files with all story variants adapted to React prop interfaces.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Reference these Vue source stories for content/structure to mirror:
@src/components/Table/Table.stories.ts
@src/components/RichTextEditor/RichTextEditor.stories.ts

Reference these React component implementations for prop interfaces:
@src/components/Table/Table.tsx
@src/components/RichTextEditor/RichTextEditor.tsx

Reference existing React stories for pattern:
@src/components/Accordion/Accordion.stories.tsx

<interfaces>
<!-- Table React interface (from Table.tsx): -->
<!-- export interface Column { label: string; value: string; sortable?: boolean; width?: string; render?: (value: any, item: any, index: number) => ReactNode } -->
<!-- export interface TableProps { columns, items, sortOptions, page, onPageChange, itemsPerPage, onItemsPerPageChange, numberOfItems, renderPaginationInBackEnd, hideFooter, isHeaderFixed, enableSelection, enableAggregation, loading, noShadow, hasHover, onSortBy, onPageItems, onSelectAll, renderSelect, renderAggregation, renderChilds, renderActions, renderEmptyState, renderFooter, renderItemsPerPage, renderShowingPage } -->
<!-- Table has compound sub-components: Table.Actions, Table.Footer, Table.EmptyState -->

<!-- RichTextEditor React interface (from RichTextEditor.tsx): -->
<!-- interface RichTextEditorProps { value, onChange, labelValue, isError, errorMessage, infoMessage, placeholder, disabled, required, tooltipMinWidth, noBorder, minHeight, maxHeight } -->

<!-- Vue slot -> React render prop / children mapping: -->
<!-- <template #slot="{ param }"> -> renderSlot={(param) => <JSX />} -->
<!-- <template #actions> -> renderActions={() => <JSX />} OR <Table.Actions>JSX</Table.Actions> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Table React stories</name>
  <files>src/components/Table/Table.stories.tsx</files>
  <action>
Create Table.stories.tsx mirroring Table.stories.ts with these story variants. First read Table.tsx to understand the exact React prop API (render props vs compound components vs direct props).

Story variants to create:
- Primary: Full-featured table with columns (Name/Age/Membership), 40+ item dataset, sorting, pagination, selection checkbox, aggregation expand, child rows, empty state, custom footer slots. Use useState for page and itemsPerPage. Use column render props for cell content.
- Options: Table with sortOptions preset (by: "name", desc: true).
- Loading: Table with loading=true showing skeleton rows.
- Selection: Table with enableSelection=true and checkbox render prop.
- Aggregation: Table with enableAggregation=true, expand/collapse child rows.
- IsHeaderFixed: Table with isHeaderFixed=true and itemsPerPage=100.
- HasHover: Table with hasHover=true for row hover effect.
- HideFooter: Table with hideFooter=true.
- NoShadow: Table with noShadow=true.
- EmptyState: Table with items=[] showing empty state content.
- Footer: Table with custom footer content.
- ItemsPerPage: Table with custom "Items per page" label.
- ShowingPage: Table with custom "Showing X-Y of Z" label.

Vue-to-React adaptations:
- `v-model:page` -> `page` + `onPageChange` with useState
- `v-model:items-per-page` -> `itemsPerPage` + `onItemsPerPageChange` with useState
- `@sort-by` -> `onSortBy` callback
- `@page-items` -> `onPageItems` callback
- `@select-all` -> `onSelectAll` callback
- Vue named slots (`<template #name="{ item }">`) -> React render props or column.render function
- `<template #select="{ item }">` -> renderSelect render prop
- `<template #aggregation="{ item }">` -> renderAggregation render prop
- `<template #childs="{ item }">` -> renderChilds render prop
- `<template #actions>` -> renderActions or Table.Actions compound
- `<template #empty-state>` -> renderEmptyState or Table.EmptyState compound
- `<template #footer>` -> renderFooter or Table.Footer compound
- `<template #items-per-page>` -> renderItemsPerPage
- `<template #showing-page="{ min, max, total }">` -> renderShowingPage
- Column cell rendering: use column.render prop function instead of Vue named slots

Copy the full 40+ item dataset from the Vue stories (items array with name, age, memberShip, selected, childs).

Import Checkbox and Icon from their respective component directories if used in stories.
  </action>
  <verify>
    <automated>npx storybook build 2>&1 | tail -20</automated>
  </verify>
  <done>Table.stories.tsx has 13 story exports (Primary, Options, Loading, Selection, Aggregation, IsHeaderFixed, HasHover, HideFooter, NoShadow, EmptyState, Footer, ItemsPerPage, ShowingPage). All use @storybook/react and React JSX.</done>
</task>

<task type="auto">
  <name>Task 2: Create RichTextEditor React stories</name>
  <files>src/components/RichTextEditor/RichTextEditor.stories.tsx</files>
  <action>
Create RichTextEditor.stories.tsx mirroring RichTextEditor.stories.ts with these story variants:
- Primary: Editor with sample HTML content (bold, italic, underline, lists, links, blockquote). Use useState for value.
- IsError: Editor with isError=true and errorMessage="Please enter valid content".
- InfoMessage: Editor with infoMessage tooltip text.
- Disabled: Editor with disabled=true.
- Required: Editor with required=true.
- NoBorder: Editor with noBorder=true.

Vue-to-React adaptations:
- `v-model` -> `value` + `onChange` with useState
- `:label-value` -> `labelValue="Rich Text Editor"`
- `:error-message` -> `errorMessage="..."`
- `:info-message` -> `infoMessage="..."`
- `:tooltip-min-width` -> `tooltipMinWidth="none"`
- `:is-error` -> `isError={true}`
- `:no-border` -> `noBorder={true}`
- `:min-height` -> `minHeight="200px"`
- `:max-height` -> `maxHeight="400px"`
- Add `className="w-full"` to match Vue `class="w-full"`

Copy the exact same default HTML content string from the Vue stories for the modelValue default.

Use `import type { Meta, StoryObj } from '@storybook/react'` (NOT vue3).
  </action>
  <verify>
    <automated>npx storybook build 2>&1 | tail -20</automated>
  </verify>
  <done>RichTextEditor.stories.tsx has 6 story exports (Primary, IsError, InfoMessage, Disabled, Required, NoBorder). All use @storybook/react and React JSX with useState for controlled value.</done>
</task>

</tasks>

<verification>
Both stories files exist and use @storybook/react imports:
```bash
grep -l "@storybook/react" src/components/Table/Table.stories.tsx src/components/RichTextEditor/RichTextEditor.stories.tsx
```

Storybook builds without errors:
```bash
npx storybook build 2>&1 | tail -5
```
</verification>

<success_criteria>
- Table.stories.tsx exists with 13 story variants matching Vue source
- RichTextEditor.stories.tsx exists with 6 story variants matching Vue source
- All files use @storybook/react, React JSX, and useState for controlled state
- Storybook build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-complex-components/07-07-SUMMARY.md`
</output>
