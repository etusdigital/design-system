---
phase: 07-complex-components
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/History/History.stories.tsx
  - src/components/Crop/Crop.stories.tsx
  - src/components/Sidebar/Sidebar.stories.tsx
  - src/components/Tree/Tree.stories.tsx
autonomous: true
requirements: [CPLX-01, CPLX-02, CPLX-03, CPLX-04]
gap_closure: true

must_haves:
  truths:
    - "History component viewable in React Storybook with all position/type/icon variants"
    - "Crop component viewable in React Storybook with image cropping demo"
    - "Sidebar component viewable in React Storybook in collapsed and expanded modes"
    - "Tree component viewable in React Storybook with single and multiple selection"
  artifacts:
    - path: "src/components/History/History.stories.tsx"
      provides: "React Storybook stories for History"
      contains: "export const Primary"
    - path: "src/components/Crop/Crop.stories.tsx"
      provides: "React Storybook stories for Crop"
      contains: "export const Primary"
    - path: "src/components/Sidebar/Sidebar.stories.tsx"
      provides: "React Storybook stories for Sidebar"
      contains: "export const Primary"
    - path: "src/components/Tree/Tree.stories.tsx"
      provides: "React Storybook stories for Tree"
      contains: "export const Primary"
  key_links:
    - from: "src/components/History/History.stories.tsx"
      to: "src/components/History/History.tsx"
      via: "import { History } from './History'"
      pattern: "import.*History.*from.*History"
    - from: "src/components/Crop/Crop.stories.tsx"
      to: "src/components/Crop/Crop.tsx"
      via: "import { Crop } from './Crop'"
      pattern: "import.*Crop.*from.*Crop"
    - from: "src/components/Sidebar/Sidebar.stories.tsx"
      to: "src/components/Sidebar/Sidebar.tsx"
      via: "import { Sidebar } from './Sidebar'"
      pattern: "import.*Sidebar.*from.*Sidebar"
    - from: "src/components/Tree/Tree.stories.tsx"
      to: "src/components/Tree/Tree.tsx"
      via: "import { Tree } from './Tree'"
      pattern: "import.*Tree.*from.*Tree"
---

<objective>
Create React Storybook stories (.stories.tsx) for History, Crop, Sidebar, and Tree components, mirroring all story variants from the existing Vue .stories.ts files.

Purpose: UAT revealed all 6 Phase 7 components are missing React Storybook stories. This plan covers the 4 simpler components.
Output: 4 new .stories.tsx files with all story variants adapted to React prop interfaces.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Reference these Vue source stories for content/structure to mirror:
@src/components/History/History.stories.ts
@src/components/Crop/Crop.stories.ts
@src/components/Sidebar/Sidebar.stories.ts
@src/components/Tree/Tree.stories.ts

Reference these React component implementations for prop interfaces:
@src/components/History/History.tsx
@src/components/Crop/Crop.tsx
@src/components/Sidebar/Sidebar.tsx
@src/components/Tree/Tree.tsx

Reference existing React stories for pattern:
@src/components/Accordion/Accordion.stories.tsx
@src/components/Carousel/Carousel.stories.tsx

<interfaces>
<!-- React story pattern (from Accordion.stories.tsx): -->
<!-- import { useState } from 'react'; -->
<!-- import type { Meta, StoryObj } from '@storybook/react'; -->
<!-- import { ComponentName } from './ComponentName'; -->
<!-- const meta = { component: ComponentName, argTypes: {...} } satisfies Meta<typeof ComponentName>; -->
<!-- export default meta; -->
<!-- type Story = StoryObj<typeof ComponentName>; -->
<!-- Stories use JSX render functions with useState for controlled state -->

<!-- Vue-to-React prop mapping: -->
<!-- v-model="args.modelValue" -> value={value} onChange={setValue} with useState -->
<!-- :prop="args.prop" -> prop={value} -->
<!-- <template #slot="{ param }"> -> renderSlot={(param) => <JSX />} -->
<!-- kebab-case props -> camelCase props -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create History and Crop React stories</name>
  <files>src/components/History/History.stories.tsx, src/components/Crop/Crop.stories.tsx</files>
  <action>
Create History.stories.tsx mirroring History.stories.ts with these story variants:
- Primary: Timeline with 6 options, renderOption render prop showing date + label with type-based colors. Use useState for value.
- Positions: Show all 4 positions (top/bottom/left/right) in a flex column layout.
- Types: Show all 6 types (primary/info/success/warning/danger/neutral) side by side.
- Disabled: Same as Primary but disabled=true.
- MultiType: Options with individual type overrides (not using global type prop).
- Icons: Options with non-round icons (isIconRound=false).
- IsIconRound: Options with round icons (isIconRound=true).
- Unfilled: Options with unfilled=true.

Vue-to-React adaptations:
- `v-model` -> `value` + `onChange` with useState
- `<template #option="{ option, index, active }">` -> `renderOption={(option, index, active) => <JSX />}`
- `v-if="index == 0"` -> `{index === 0 && <span>Current</span>}`
- Vue class binding objects -> clsx or template literals
- Use React prop names from HistoryProps interface (value, onChange, options, position, type, disabled, renderOption)

Create Crop.stories.tsx mirroring Crop.stories.ts:
- Primary: Show crop component with banner.jpg source, display the cropped output below.
- Use useState for the output value.
- Import banner from './banner.jpg'.
- Vue-to-React: `v-model` -> `value` + `onChange` with useState; show cropped image via `{value && <img src={value} />}`
- Use React prop names from CropProps interface (value, onChange, src, width, height)

Both files must use `import type { Meta, StoryObj } from '@storybook/react'` (NOT vue3).
  </action>
  <verify>
    <automated>npx storybook build 2>&1 | tail -20</automated>
  </verify>
  <done>History.stories.tsx has 7 story exports (Primary, Positions, Types, Disabled, MultiType, Icons, IsIconRound, Unfilled). Crop.stories.tsx has Primary story. Both import from @storybook/react.</done>
</task>

<task type="auto">
  <name>Task 2: Create Sidebar and Tree React stories</name>
  <files>src/components/Sidebar/Sidebar.stories.tsx, src/components/Tree/Tree.stories.tsx</files>
  <action>
Create Sidebar.stories.tsx mirroring Sidebar.stories.ts with these story variants:
- Primary: Collapsed sidebar with navigation options (Dashboard, Projects with nested sub-options, Team, Analytics disabled, Settings bottom). Use useState for value.
- Expanded: Same options but expanded=true.

Vue-to-React adaptations:
- `v-model` -> `value` + `onChange` with useState
- `:expanded` -> `expanded={true}`
- `:options` -> `options={[...]}`
- `:get-object` -> `getObject={false}`
- Wrap in a div with h-screen for proper height
- Use React prop names from SidebarProps interface (value, onChange, expanded, options, getObject)

Create Tree.stories.tsx mirroring Tree.stories.ts with these story variants:
- Primary: Tree with Documents/Downloads/Music hierarchy, each with nested children. Use useState for value.
- Multiple: Same tree with multiple=true for checkbox selection mode.
- Disabled: Same tree with disabled=true.

Vue-to-React adaptations:
- `v-model` -> `value` + `onChange` with useState
- `:options` -> `options={[...]}`
- `:label-key` -> `labelKey="label"`
- `:value-key` -> `valueKey="value"`
- `:get-object` -> `getObject={false}`
- `:multiple` -> `multiple={true}`
- Use React prop names from TreeProps interface (value, onChange, options, labelKey, valueKey, getObject, multiple, disabled)

Both files must use `import type { Meta, StoryObj } from '@storybook/react'` (NOT vue3).
  </action>
  <verify>
    <automated>npx storybook build 2>&1 | tail -20</automated>
  </verify>
  <done>Sidebar.stories.tsx has Primary and Expanded stories. Tree.stories.tsx has Primary, Multiple, and Disabled stories. Both import from @storybook/react and use React JSX syntax.</done>
</task>

</tasks>

<verification>
All 4 stories files exist and use @storybook/react imports:
```bash
grep -l "@storybook/react" src/components/History/History.stories.tsx src/components/Crop/Crop.stories.tsx src/components/Sidebar/Sidebar.stories.tsx src/components/Tree/Tree.stories.tsx
```

Storybook builds without errors:
```bash
npx storybook build 2>&1 | tail -5
```
</verification>

<success_criteria>
- History.stories.tsx exists with 8 story variants matching Vue source
- Crop.stories.tsx exists with Primary story matching Vue source
- Sidebar.stories.tsx exists with Primary and Expanded stories matching Vue source
- Tree.stories.tsx exists with Primary, Multiple, and Disabled stories matching Vue source
- All files use @storybook/react, React JSX, and useState for controlled state
- Storybook build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-complex-components/07-06-SUMMARY.md`
</output>
