---
phase: 07-complex-components
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/components/Overlay.test.tsx
  - src/utils/components/Container.test.tsx
  - src/utils/components/ExpandableContainer.test.tsx
  - src/utils/components/SelectContainer.test.tsx
  - src/hooks/useTransition.test.ts
  - src/components/Icon/Icon.test.tsx
  - src/components/Textarea/Textarea.test.tsx
  - src/components/Slider/Slider.test.tsx
autonomous: true
gap_closure: true
requirements: [CPLX-01, CPLX-02, CPLX-03, CPLX-04, CPLX-05, CPLX-06]

must_haves:
  truths:
    - "All 8 test files pass without modifying production component code"
    - "Tests are updated to match actual component interfaces, not the other way around"
  artifacts:
    - path: "src/utils/components/Overlay.test.tsx"
      provides: "Fixed prop name from modelValue to value"
    - path: "src/utils/components/Container.test.tsx"
      provides: "Portal-aware content queries"
    - path: "src/utils/components/ExpandableContainer.test.tsx"
      provides: "Portal-aware shadow queries, removed alignRight assertion"
    - path: "src/utils/components/SelectContainer.test.tsx"
      provides: "Portal-aware content queries"
    - path: "src/hooks/useTransition.test.ts"
      provides: "Fixed timing expectations"
    - path: "src/components/Icon/Icon.test.tsx"
      provides: "Fixed fontSize assertion"
    - path: "src/components/Textarea/Textarea.test.tsx"
      provides: "Fixed counter assertion and value prop"
    - path: "src/components/Slider/Slider.test.tsx"
      provides: "Fixed defaultValue prop assertions"
  key_links: []
---

<objective>
Fix 8 failing test files for internal utility components, hooks, and form components. Tests must be updated to match actual component interfaces (prop names, DOM structure, portal rendering) — not the other way around.

Purpose: Close the unit test portion of UAT gap 8 (part 1 of 2).
Output: 8 test files passing, ~17 test failures resolved.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Overlay, Container, ExpandableContainer, SelectContainer tests (portal + prop mismatches)</name>
  <files>src/utils/components/Overlay.test.tsx, src/utils/components/Container.test.tsx, src/utils/components/ExpandableContainer.test.tsx, src/utils/components/SelectContainer.test.tsx</files>
  <action>
For each file, read the test AND the corresponding production component to understand the actual interface, then fix the test. Do NOT modify production code.

**Overlay.test.tsx** (3 failures):
- Root cause: Tests use `modelValue` prop but component prop is named `value`.
- Fix: Change all `modelValue` prop occurrences to `value` in the test file.
- Also fix `onClick` assertion if the component's backdrop click callback has a different name — read Overlay.tsx to verify the actual prop name.

**Container.test.tsx** (2 failures):
- Root cause: FloatCard portal renders content to document.body; `container.querySelectorAll` misses portal content.
- Fix: For tests checking rendered content, use `document.querySelector` or `screen.getByText` instead of scoping to the render container. Read Container.tsx to verify what gets portaled vs what stays in-tree.
- Also check if `renderContent` is a prop or an internal function — match test assertions to actual API.

**ExpandableContainer.test.tsx** (2 failures):
- Root cause 1: Shadow class renders inside portal, not within the test container.
- Root cause 2: `alignRight` prop was never implemented on the component.
- Fix 1: Query `document` instead of container for shadow class when expanded.
- Fix 2: Remove the `alignRight` test entirely (or replace with a test for a prop that exists). Read ExpandableContainer.tsx to verify which props exist.

**SelectContainer.test.tsx** (2 failures):
- Root cause: Content renders via portal; `container.querySelector` misses portal content.
- Fix: Use `document.querySelector` or RTL screen queries for portal-rendered content.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npx vitest --project unit --run src/utils/components/Overlay.test.tsx src/utils/components/Container.test.tsx src/utils/components/ExpandableContainer.test.tsx src/utils/components/SelectContainer.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 utility component test files pass. Tests query portal content via document-level queries. Overlay uses value (not modelValue). No production code changed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix useTransition, Icon, Textarea, Slider tests (timing + prop mismatches)</name>
  <files>src/hooks/useTransition.test.ts, src/components/Icon/Icon.test.tsx, src/components/Textarea/Textarea.test.tsx, src/components/Slider/Slider.test.tsx</files>
  <action>
For each file, read the test AND the production code, then fix the test only.

**useTransition.test.ts** (3 failures):
- Root cause: Tests expect specific RAF timing behavior that doesn't match the actual hook implementation.
- Fix: Read useTransition.ts to understand actual timing. The hook likely uses requestAnimationFrame for opening and a timeout/immediate for closing. Update test expectations to match actual behavior. May need to properly mock requestAnimationFrame or use vi.useFakeTimers with correct advance amounts.

**Icon.test.tsx** (2 failures):
- Root cause: Tests check `fontSize` from size prop, but Icon likely uses a different mechanism (CSS class or style).
- Fix: Read Icon.tsx to see how size is applied. The Icon component renders a Material Symbols span. If it uses `style={{ fontSize }}`, verify the default is correct. If it uses a CSS class, update the assertion to check the class instead of inline style.

**Textarea.test.tsx** (1 failure):
- Root cause: Test uses `defaultValue` prop but Textarea uses `value` via useControllable; counter shows `0/100` not `4/100`.
- Fix: Use `value="test"` instead of `defaultValue="test"` (since useControllable hardcodes `defaultValue:''`). Or check if Textarea actually accepts defaultValue — read the component to verify. The counter format may be `currentLength/max` — verify in component and match test expectation.

**Slider.test.tsx** (2 failures):
- Root cause: Tests pass `defaultValue` prop to Slider but Slider's useControllable may hardcode `defaultValue:0`.
- Fix: Read Slider.tsx to verify if defaultValue prop is accepted. Per STATE.md decision: "Slider defaultValue uses ?? (nullish coalescing) so explicit 0 from consumer is honored". If defaultValue IS supported, the test may be asserting the wrong thing. If not, use `value` prop instead. Adjust vi.spyOn assertions to match actual hook call signature.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npx vitest --project unit --run src/hooks/useTransition.test.ts src/components/Icon/Icon.test.tsx src/components/Textarea/Textarea.test.tsx src/components/Slider/Slider.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 test files pass. useTransition timing matches actual hook behavior. Icon size assertions match actual rendering mechanism. Textarea uses correct prop names and counter format. Slider defaultValue assertions match actual useControllable integration. No production code changed.</done>
</task>

</tasks>

<verification>
- `npx vitest --project unit --run src/utils/components/ src/hooks/useTransition.test.ts src/components/Icon/Icon.test.tsx src/components/Textarea/Textarea.test.tsx src/components/Slider/Slider.test.tsx` — all pass
- No production component files modified (only .test.tsx files)
</verification>

<success_criteria>
- All 8 test files pass (17 individual test failures resolved)
- Zero production component code changes
- Tests accurately reflect actual component interfaces
</success_criteria>

<output>
After completion, create `.planning/phases/07-complex-components/07-10-SUMMARY.md`
</output>
