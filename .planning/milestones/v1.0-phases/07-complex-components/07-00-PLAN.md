---
phase: 07-complex-components
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/Table/Table.test.tsx
  - src/components/Tree/Tree.test.tsx
  - src/components/Sidebar/Sidebar.test.tsx
  - src/components/RichTextEditor/RichTextEditor.test.tsx
  - src/components/Crop/Crop.test.tsx
  - src/components/History/History.test.tsx
autonomous: true
requirements: [CPLX-01, CPLX-02, CPLX-03, CPLX-04, CPLX-05, CPLX-06]

must_haves:
  truths:
    - "All 6 complex component test files exist and pass (smoke tests)"
  artifacts:
    - path: "src/components/Table/Table.test.tsx"
      provides: "Table smoke test"
      contains: "document.body"
    - path: "src/components/Tree/Tree.test.tsx"
      provides: "Tree smoke test"
      contains: "document.body"
    - path: "src/components/Sidebar/Sidebar.test.tsx"
      provides: "Sidebar smoke test"
      contains: "document.body"
    - path: "src/components/RichTextEditor/RichTextEditor.test.tsx"
      provides: "RichTextEditor smoke test"
      contains: "document.body"
    - path: "src/components/Crop/Crop.test.tsx"
      provides: "Crop smoke test"
      contains: "document.body"
    - path: "src/components/History/History.test.tsx"
      provides: "History smoke test"
      contains: "document.body"
  key_links: []
---

<objective>
Create smoke test stubs for all 6 complex components (Table, Tree, Sidebar, RichTextEditor, Crop, History) so that every subsequent plan has an automated verification target.

Purpose: Nyquist compliance — every plan that modifies a component must have a test file to verify against. These stubs import from `./index`, render the component, and assert `document.body` is truthy.

Output: 6 test files following the established Phase 2 smoke test pattern.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-complex-components/07-CONTEXT.md
@.planning/phases/07-complex-components/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 smoke test stubs for all complex components</name>
  <files>
    src/components/Table/Table.test.tsx,
    src/components/Tree/Tree.test.tsx,
    src/components/Sidebar/Sidebar.test.tsx,
    src/components/RichTextEditor/RichTextEditor.test.tsx,
    src/components/Crop/Crop.test.tsx,
    src/components/History/History.test.tsx
  </files>
  <read_first>
    src/components/Dropdown/Dropdown.test.tsx
  </read_first>
  <action>
Create 6 test files following the established smoke test pattern from Phase 2 (02-00).

Each file follows this exact template (substitute ComponentName):

```tsx
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { ComponentName } from './index';

describe('ComponentName', () => {
  it('mounts without crashing', () => {
    render(<ComponentName />);
    expect(document.body).toBeTruthy();
  });
});
```

Components to create test files for:
1. `Table` — import from `./index`, render `<Table columns={[]} items={[]} />`
2. `Tree` — import from `./index`, render `<Tree options={[]} />`
3. `Sidebar` — import from `./index`, render `<Sidebar options={[]} />`
4. `RichTextEditor` — import from `./index`, render `<RichTextEditor />`
5. `Crop` — import from `./index`, render `<Crop />`
6. `History` — import from `./index`, render `<History options={[]} />`

Note: The current stubs accept `Record<string, unknown>` props, so passing required props as empty arrays is fine. These tests will be enhanced when the real implementations land.
  </action>
  <verify>
    <automated>npx vitest run --project unit src/components/Table/Table.test.tsx src/components/Tree/Tree.test.tsx src/components/Sidebar/Sidebar.test.tsx src/components/RichTextEditor/RichTextEditor.test.tsx src/components/Crop/Crop.test.tsx src/components/History/History.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Table/Table.test.tsx contains "import { Table } from './index'"
    - src/components/Tree/Tree.test.tsx contains "import { Tree } from './index'"
    - src/components/Sidebar/Sidebar.test.tsx contains "import { Sidebar } from './index'"
    - src/components/RichTextEditor/RichTextEditor.test.tsx contains "import { RichTextEditor } from './index'"
    - src/components/Crop/Crop.test.tsx contains "import { Crop } from './index'"
    - src/components/History/History.test.tsx contains "import { History } from './index'"
    - All 6 test files contain "document.body"
    - All 6 tests pass via vitest run --project unit
  </acceptance_criteria>
  <done>All 6 smoke test files exist, import from ./index, render the component, and pass green.</done>
</task>

</tasks>

<verification>
npx vitest run --project unit src/components/Table/Table.test.tsx src/components/Tree/Tree.test.tsx src/components/Sidebar/Sidebar.test.tsx src/components/RichTextEditor/RichTextEditor.test.tsx src/components/Crop/Crop.test.tsx src/components/History/History.test.tsx
</verification>

<success_criteria>
All 6 test files exist and all smoke tests pass green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-complex-components/07-00-SUMMARY.md`
</output>
