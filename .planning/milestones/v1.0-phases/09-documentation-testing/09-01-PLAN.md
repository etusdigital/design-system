---
phase: 09-documentation-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - TEST-01
  - TEST-03

must_haves:
  truths:
    - "npm test runs vitest and returns an exit code"
    - "npm run test:watch starts vitest in watch mode"
    - "npm run build-storybook completes with zero errors"
    - "vitest run completes with zero failures"
  artifacts:
    - path: "package.json"
      provides: "test and test:watch scripts"
      contains: '"test": "vitest run"'
  key_links:
    - from: "package.json"
      to: "vite.config.ts"
      via: "vitest run reads test config from vite.config.ts"
      pattern: "vitest run"
---

<objective>
Add missing test scripts to package.json, run baseline vitest to discover and fix any failing tests, and validate Storybook builds cleanly.

Purpose: Establishes the test infrastructure gate (TEST-01, TEST-03) so form component test work (plan 02) can rely on a green baseline.
Output: package.json with test scripts, all existing tests passing, build-storybook confirmed green.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-testing/09-CONTEXT.md
@.planning/phases/09-documentation-testing/09-RESEARCH.md
@package.json
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test scripts to package.json and fix baseline failures</name>
  <files>package.json</files>
  <read_first>
    - package.json (current scripts section -- confirm test/test:watch are absent)
    - vite.config.ts (confirm unit project name and config)
  </read_first>
  <action>
1. Open package.json and add these two entries to the "scripts" object per user decision:
   - `"test": "vitest run"` -- runs the full test suite
   - `"test:watch": "vitest"` -- watch mode

   Place them after the "prepare" entry.

2. Run `npm test` to execute the test suite. Capture the output.

   IMPORTANT: If bare `vitest run` fails because the storybook test project
   triggers a playwright browser download or missing dependency error, fall back
   to scoped commands:
   - `"test": "vitest run --project unit"`
   - `"test:watch": "vitest --project unit"`
   Log this deviation in the SUMMARY so the user is aware.

3. If any tests fail (real test failures, not infrastructure issues):
   - Read the failing test file AND the component source file it tests
   - Fix the TEST file to match the component's actual behavior (per project rule: fix tests, never change components for tests)
   - Re-run `npm test` until all tests pass

4. Run `npm run build-storybook` to validate all 57 stories build without errors.
   - If build-storybook fails, read the error output and fix the offending story or config file
   - Do NOT modify component source files -- only fix story/config issues
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"test": "vitest run"` (or scoped variant if bare form fails -- see step 2)
    - package.json contains `"test:watch": "vitest"` (or scoped variant if bare form fails -- see step 2)
    - `npm test` exits with code 0 (all unit tests pass)
    - `npm run build-storybook` exits with code 0 (all stories build)
  </acceptance_criteria>
  <done>package.json has test scripts, all unit tests pass, build-storybook succeeds</done>
</task>

</tasks>

<verification>
- `grep '"test":' package.json` shows the test script
- `grep '"test:watch":' package.json` shows the watch script
- `npm test` exits 0
- `npm run build-storybook` exits 0
</verification>

<success_criteria>
- package.json has both test scripts
- All existing unit tests pass (zero failures)
- build-storybook completes without errors
- No component source files were modified
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation-testing/09-01-SUMMARY.md`
</output>
