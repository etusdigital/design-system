---
phase: 07-complex-components
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RoundMenu/RoundMenu.test.tsx
  - src/components/Stepper/Stepper.test.tsx
  - src/components/Select/Select.test.tsx
  - src/components/AutoComplete/AutoComplete.test.tsx
  - src/components/TagSelect/TagSelect.test.tsx
  - src/components/Filter/Filter.test.tsx
  - src/components/Dropdown/Dropdown.test.tsx
  - src/components/Navbar/Navbar.test.tsx
  - src/components/DatePicker/DatePicker.test.tsx
autonomous: true
gap_closure: true
requirements: [CPLX-01, CPLX-02, CPLX-03, CPLX-04, CPLX-05, CPLX-06]

must_haves:
  truths:
    - "All 9 test files pass without modifying production component code"
    - "Tests are updated to match actual component interfaces, not the other way around"
  artifacts:
    - path: "src/components/RoundMenu/RoundMenu.test.tsx"
      provides: "Fixed trigger query and aria assertions"
    - path: "src/components/Stepper/Stepper.test.tsx"
      provides: "Fixed class queries, value prop, onChange signature"
    - path: "src/components/Select/Select.test.tsx"
      provides: "Portal-aware option queries"
    - path: "src/components/AutoComplete/AutoComplete.test.tsx"
      provides: "Portal-aware option queries"
    - path: "src/components/TagSelect/TagSelect.test.tsx"
      provides: "Portal-aware option queries"
    - path: "src/components/Filter/Filter.test.tsx"
      provides: "Fixed expand/collapse assertions"
    - path: "src/components/Dropdown/Dropdown.test.tsx"
      provides: "Fixed to match actual component interface"
    - path: "src/components/Navbar/Navbar.test.tsx"
      provides: "Fixed to match actual component interface"
    - path: "src/components/DatePicker/DatePicker.test.tsx"
      provides: "Fixed labelValue assertion"
  key_links: []
---

<objective>
Fix 9 failing test files for composite components. Tests must be updated to match actual component interfaces (prop names, DOM queries, portal content, callback signatures) — not the other way around.

Purpose: Close the unit test portion of UAT gap 8 (part 2 of 2).
Output: 9 test files passing, ~31 test failures resolved.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RoundMenu, Stepper, Filter, DatePicker tests</name>
  <files>src/components/RoundMenu/RoundMenu.test.tsx, src/components/Stepper/Stepper.test.tsx, src/components/Filter/Filter.test.tsx, src/components/DatePicker/DatePicker.test.tsx</files>
  <action>
For each file, read the test AND the corresponding production component to understand the actual interface, then fix the test. Do NOT modify production code.

**RoundMenu.test.tsx** (6 failures):
- Root cause: Tests query `aria-label='Open menu'` but trigger has no aria-label; tests check `aria-expanded` which doesn't exist.
- Fix: Read RoundMenu.tsx to find how the trigger is rendered. Query trigger by CSS class (e.g., the button or trigger element's class). Remove `aria-expanded` assertions. For menu items, check actual DOM structure and query accordingly. Per STATE.md: "RoundMenu options use onClick key instead of Vue action key".

**Stepper.test.tsx** (4 failures):
- Root cause: Queries `[class*='circle']` but class is 'step'; `defaultValue` prop doesn't exist; `onChange` called with label string not index; `--stepper-bg` CSS variable doesn't exist.
- Fix: Read Stepper.tsx. Per STATE.md: "Stepper uses index-based active state (not value-based)", "Stepper drops version prop entirely". Query by `[class*='step']` instead of `[class*='circle']`. Use `value` prop (0-based index). Expect onChange to receive index number. Remove `--stepper-bg` CSS variable assertion or replace with actual mechanism.

**Filter.test.tsx** (3 failures):
- Root cause: Tests expect sub-options to be hidden by default (collapsed) and toggleable, but the actual component may use a different mechanism.
- Fix: Read Filter.tsx. Check if it uses CSS max-height transitions, conditional rendering, or ExpandableContainer. Update assertions to match actual expand/collapse mechanism. The "does NOT use useTransition" test may need to verify the actual transition approach used.

**DatePicker.test.tsx** (1 failure):
- Root cause: Test expects `labelValue` rendered in trigger when no date selected, but the actual component may render it differently.
- Fix: Read DatePicker.tsx. Check how the trigger label is rendered — it may use a placeholder prop or a different text element. Update the query to match actual DOM output.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npx vitest --project unit --run src/components/RoundMenu/RoundMenu.test.tsx src/components/Stepper/Stepper.test.tsx src/components/Filter/Filter.test.tsx src/components/DatePicker/DatePicker.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 test files pass. RoundMenu trigger queried by actual DOM structure. Stepper uses index-based value and correct class queries. Filter expand/collapse tests match actual mechanism. DatePicker label assertion matches actual rendering. No production code changed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Select, AutoComplete, TagSelect, Dropdown, Navbar tests</name>
  <files>src/components/Select/Select.test.tsx, src/components/AutoComplete/AutoComplete.test.tsx, src/components/TagSelect/TagSelect.test.tsx, src/components/Dropdown/Dropdown.test.tsx, src/components/Navbar/Navbar.test.tsx</files>
  <action>
For each file, read the test AND the corresponding production component to understand the actual interface, then fix the test. Do NOT modify production code.

**Select.test.tsx** (6 failures):
- Root cause: Options render inside FloatCard portal (to document.body); `container.querySelectorAll` misses them. Same portal issue as TagSelect/AutoComplete.
- Fix: Read Select.tsx. For option queries, use `document.querySelectorAll` or `screen.getAllByRole('option')` (if role is set) or `screen.getAllByText(...)` instead of container-scoped queries. For multiple mode, verify Checkbox rendering in portal content. For getObject mode, verify the actual emission format.

**AutoComplete.test.tsx** (5 failures):
- Root cause: Same portal issue — options render inside portal content. Tests use container-scoped queries that miss portaled options.
- Fix: Read AutoComplete.tsx. Use `document` or `screen` queries for option elements rendered in portal. For search filtering, verify the actual filter mechanism and query visible options via document-level queries. For no-results message, query document.

**TagSelect.test.tsx** (4 failures):
- Root cause: Same portal issue as Select. Options open in FloatCard portal.
- Fix: Read TagSelect.tsx. Use `screen.getAllByRole('option')` or `document.querySelectorAll` for portal-rendered options. For search/filter tests, use document-level queries. Per STATE.md: "TagSelect expanded internal state converted to useControllable".

**Dropdown.test.tsx** (1 failure — file-level crash):
- Root cause: The entire test file crashes (syntax error or import failure). The `[ src/components/Dropdown/Dropdown.test.tsx ]` notation in vitest output indicates a file-level error.
- Fix: Read the test file and the error output. Check for import errors, TypeScript issues, or missing mocks. Fix the file-level issue first, then verify individual tests pass.

**Navbar.test.tsx** (1 failure — file-level crash):
- Root cause: Same as Dropdown — entire file crashes.
- Fix: Read the test file and error output. Fix import/compilation issues. Per STATE.md: "Navbar DefaultLogo uses gradient SVG from Vue source; Avatar size='small' (not 'sm')".
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npx vitest --project unit --run src/components/Select/Select.test.tsx src/components/AutoComplete/AutoComplete.test.tsx src/components/TagSelect/TagSelect.test.tsx src/components/Dropdown/Dropdown.test.tsx src/components/Navbar/Navbar.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 test files pass. Select/AutoComplete/TagSelect use document-level queries for portal content. Dropdown and Navbar file-level crashes resolved. No production code changed.</done>
</task>

</tasks>

<verification>
- `npx vitest --project unit --run` — ALL 378 tests pass (0 failures)
- No production component files modified (only .test.tsx files)
- Test count should remain at 378 (no tests removed, only fixed)
</verification>

<success_criteria>
- All 9 composite component test files pass (~31 individual failures resolved)
- Combined with plan 07-10: all 45 test failures across 17 files resolved
- Zero production component code changes across both test fix plans
- Full test suite: 378 tests passing, 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/07-complex-components/07-11-SUMMARY.md`
</output>
