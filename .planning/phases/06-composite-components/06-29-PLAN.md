---
phase: 06-composite-components
plan: 29
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Calendar/Calendar.tsx
  - src/components/Calendar/Calendar.module.css
autonomous: true
requirements: [COMP-10]
gap_closure: true

must_haves:
  truths:
    - "CalendarDateDialog is absolutely positioned over the calendar grid"
    - "Calendar dialog opens defaulting to month selection panel"
    - "Compare mode allows selecting two separate date ranges"
    - "Hovering over days while selecting a range shows light highlight between start date and hovered date"
    - "Compare mode shows first range darker, second range lighter"
  artifacts:
    - path: "src/components/Calendar/Calendar.tsx"
      provides: "Absolute dialog, default month panel, compare range logic, hover state"
    - path: "src/components/Calendar/Calendar.module.css"
      provides: "Dialog absolute positioning, rangeHover, rangeSecondary classes"
  key_links:
    - from: "Calendar.tsx compare click logic"
      to: "ranges[0] and ranges[1]"
      via: "activeRangeIndex variable routing clicks to correct range"
      pattern: "activeRangeIndex\|ranges\\[1\\]"
    - from: "Calendar.tsx hoveredDate state"
      to: "getDayProps"
      via: "mouse handlers set hoveredDate, getDayProps computes highlight"
      pattern: "hoveredDate\|onMouseEnter"
---

<objective>
Fix Calendar dialog positioning, compare mode, and hover range highlighting.

Purpose: CalendarDateDialog renders in-flow (not absolute). activePanel defaults to null (should be 'month'). Compare click logic always targets range0 (never range1). No hoveredDate state or hover preview. getDayProps ignores ranges[1]. UAT round 3 test 11 (blocker).
Output: Properly positioned dialog with working compare mode and hover range highlighting.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Calendar/Calendar.tsx
@src/components/Calendar/Calendar.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Calendar dialog positioning and default panel</name>
  <files>src/components/Calendar/Calendar.tsx, src/components/Calendar/Calendar.module.css</files>
  <action>
Two fixes:

1. **Absolute dialog positioning** (Calendar.module.css): Add positioning to the .dateDialog class:
```css
.dateDialog {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  z-index: 10;
}
```
Also add `position: relative` to the parent .calendar class so the dialog positions relative to it.

2. **Default activePanel to 'month'** (Calendar.tsx line 100): Change:
```typescript
const [activePanel, setActivePanel] = useState<'month' | 'year' | null>(null);
```
to:
```typescript
const [activePanel, setActivePanel] = useState<'month' | 'year' | null>('month');
```
This ensures the dialog opens showing the month selection panel by default.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "activePanel.*useState\|position.*absolute\|position.*relative" src/components/Calendar/Calendar.tsx src/components/Calendar/Calendar.module.css | head -5</automated>
  </verify>
  <done>CalendarDateDialog is absolutely positioned. Opens with month panel visible by default.</done>
</task>

<task type="auto">
  <name>Task 2: Implement compare mode and hover range highlighting</name>
  <files>src/components/Calendar/Calendar.tsx, src/components/Calendar/Calendar.module.css</files>
  <action>
Three fixes in Calendar.tsx:

1. **Fix compare click logic** (lines 363-378): The current code always targets ranges[0]. Add an `activeRangeIndex` tracking variable (use state or derive from ranges completion):
   - When ranges[0] is incomplete (missing start or end), route clicks to ranges[0]
   - When ranges[0] is complete AND compare mode is active, route clicks to ranges[1]
   - When ranges[1] is also complete, clicking starts over (reset to ranges[0])

   Implementation: Add state `const [activeRangeIndex, setActiveRangeIndex] = useState(0)`. In the click handler, use activeRangeIndex to determine which range to update. After completing a range (both start and end set), increment activeRangeIndex. Vue uses an index variable for this exact purpose.

2. **Add hoveredDate state and mouse handlers**:
   - Add `const [hoveredDate, setHoveredDate] = useState<Date | null>(null)`
   - In the day cell rendering, add `onMouseEnter={() => setHoveredDate(date)}` and `onMouseLeave={() => setHoveredDate(null)}`
   - In getDayProps, when a range has start but no end yet, compute if the current date falls between the start date and hoveredDate. If so, add a `rangeHover: true` flag.

3. **Add secondary range support to getDayProps**: Currently only reads ranges[0]. Extend to also check ranges[1]:
   - For ranges[1], compute same flags (isStart, isEnd, inRange) but with "secondary" prefix
   - Add CSS classes: `isSecondaryStart`, `isSecondaryEnd`, `inSecondaryRange`
   - First range uses primary/darker colors, second range uses secondary/lighter colors

   CSS additions to Calendar.module.css:
   ```css
   .rangeHover {
     background-color: var(--primary-surface-default);
   }
   .inSecondaryRange {
     background-color: var(--primary-surface-light, rgba(var(--primary-rgb), 0.1));
   }
   .selectedSecondary {
     background-color: var(--primary-surface-default);
     color: var(--primary-text-default);
   }
   ```
   Use lighter opacity/token for secondary range vs the existing primary range styling.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "hoveredDate\|activeRangeIndex\|Secondary\|rangeHover" src/components/Calendar/Calendar.tsx src/components/Calendar/Calendar.module.css | head -10</automated>
  </verify>
  <done>Compare mode routes clicks to range0 then range1. Hover shows preview highlight. Secondary range has lighter styling.</done>
</task>

</tasks>

<verification>
- Calendar dialog: appears absolutely overlaid on the calendar grid, shows month panel by default
- Calendar range mode: select start date, hover over other dates, see light highlight between them
- Calendar compare mode: select first range (2 clicks), then second range routes to different range with lighter color
</verification>

<success_criteria>
- Dialog absolutely positioned with month panel default
- Compare mode selects two independent ranges with different visual intensities
- Hover preview highlights days between start and hovered date
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-29-SUMMARY.md`
</output>
