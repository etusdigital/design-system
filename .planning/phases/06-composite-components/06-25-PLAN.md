---
phase: 06-composite-components
plan: 25
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AutoComplete/AutoComplete.tsx
  - src/utils/components/SelectContent.tsx
  - src/components/Input/Input.tsx
autonomous: true
requirements: [COMP-02, COMP-15]
gap_closure: true

must_haves:
  truths:
    - "AutoComplete selecting an option updates the displayed text to the selected option's label"
    - "AutoComplete shows unfold_more icon on the right side"
    - "Input type=color shows hex text on left, color swatch on right inside input border"
  artifacts:
    - path: "src/components/AutoComplete/AutoComplete.tsx"
      provides: "Synced searchText from model, unfold_more icon"
    - path: "src/components/Input/Input.tsx"
      provides: "Color swatch positioned on right side of input"
  key_links:
    - from: "src/components/AutoComplete/AutoComplete.tsx"
      to: "searchText state"
      via: "useEffect syncing searchText from model changes"
      pattern: "useEffect.*model"
---

<objective>
Fix AutoComplete option selection sync and icon, plus Input color swatch position.

Purpose: AutoComplete searchText and model are decoupled -- selecting an option updates model but display stays stale. No unfold_more icon rendered. Input type=color has swatch on wrong side. UAT round 3 tests 6 and 15.
Output: Working AutoComplete selection with correct icon, correct Input color layout.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/AutoComplete/AutoComplete.tsx
@src/utils/components/SelectContent.tsx
@src/utils/components/Container.tsx
@src/components/Input/Input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AutoComplete selection sync and unfold_more icon</name>
  <files>src/components/AutoComplete/AutoComplete.tsx, src/utils/components/SelectContent.tsx</files>
  <action>
Two fixes in AutoComplete.tsx:

1. **Selection sync**: Add a useEffect that syncs searchText from model when model changes. When model has a value, find the matching option and set searchText to its label:
```typescript
useEffect(() => {
  if (model !== undefined && model !== null && model !== '') {
    const matched = options.find(o => getValue(o) === (isObject(model) ? getValue(model) : model));
    if (matched) setSearchText(getLabel(matched));
  } else {
    setSearchText('');
  }
}, [model]);
```
This ensures that after selectOption calls setModel, the display text updates.

2. **unfold_more icon**: Pass `icon="unfold_more"` to the SelectContainer or Container component used by AutoComplete. Check how AutoComplete renders its trigger -- if it uses SelectContent with searchable=true (which hardcodes search icon), override the icon. The Vue version uses Input with icon='unfold_more' as an append-icon. In the React version, pass the unfold_more icon to whatever renders the right-side icon in the trigger area. If SelectContent line 94 hardcodes the search icon for searchable mode, add an `icon` prop override that AutoComplete can use to pass "unfold_more".

In SelectContent.tsx: If searchable is true AND an icon override prop is provided, use the override instead of the search icon.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "unfold_more\|useEffect.*model" src/components/AutoComplete/AutoComplete.tsx | head -5</automated>
  </verify>
  <done>Selecting an AutoComplete option updates displayed text. unfold_more icon visible on right side.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Input type=color swatch position</name>
  <files>src/components/Input/Input.tsx</files>
  <action>
In Input.tsx lines 202-237, the FloatCard (color swatch trigger) is rendered BEFORE the input element in JSX. Flex order puts swatch on left, text on right. The user expects hex text on left, color swatch on far right within the input field.

Fix: Swap the JSX order so the input (hex text) renders first, then the FloatCard (swatch) renders second. This puts the swatch on the right side via natural flex order.

Specifically, in the color type branch of the render:
- First: render the text input for hex value
- Second: render the FloatCard wrapping the color swatch button

No CSS changes needed -- just JSX order swap within the flex container.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "FloatCard\|colorInput\|type.*color" src/components/Input/Input.tsx | head -10</automated>
  </verify>
  <done>Input type=color shows hex text on left, color swatch square on right within the input border.</done>
</task>

</tasks>

<verification>
- AutoComplete story: type to filter, select option, verify text updates to selected label
- AutoComplete story: verify unfold_more icon visible on right
- Input color story: verify hex text on left, swatch on right inside input
</verification>

<success_criteria>
- AutoComplete option selection updates display text
- AutoComplete shows unfold_more icon
- Input color swatch on right side of input field
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-25-SUMMARY.md`
</output>
