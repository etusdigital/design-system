---
phase: 06-composite-components
plan: 30
type: execute
wave: 2
depends_on: [29]
files_modified:
  - src/components/DatePicker/DatePicker.module.css
  - src/components/DatePicker/DatePicker.tsx
  - src/components/Calendar/Calendar.module.css
autonomous: true
requirements: [COMP-11]
gap_closure: true

must_haves:
  truths:
    - "DatePicker card has professional proportions with adequate whitespace"
    - "Day cells are large enough to be easily clickable (min 36x36px)"
    - "Trigger button has adequate minimum width for date text display"
    - "Clear and Apply buttons are properly sized with horizontal padding"
  artifacts:
    - path: "src/components/DatePicker/DatePicker.module.css"
      provides: "Larger card with min-width, padded actions"
    - path: "src/components/Calendar/Calendar.module.css"
      provides: "Larger day cells with p-xs and min dimensions"
    - path: "src/components/DatePicker/DatePicker.tsx"
      provides: "Trigger min-width, calendar wrapper padding"
  key_links:
    - from: "DatePicker.tsx"
      to: "Calendar component"
      via: "Calendar rendered inside DatePicker card with wrapper padding"
      pattern: "Calendar"
---

<objective>
Fix DatePicker proportions, spacing, and overall visual sizing.

Purpose: DatePicker is too small and cramped -- card has only p-sm with no min-width, day cells use p-xxs, actions have no horizontal padding, trigger has no min-width, calendar wrapper missing padding. UAT round 3 test 12 (blocker).
Output: Professional DatePicker layout with proper proportions matching Vue version.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/DatePicker/DatePicker.tsx
@src/components/DatePicker/DatePicker.module.css
@src/components/Calendar/Calendar.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DatePicker and Calendar proportions</name>
  <files>src/components/DatePicker/DatePicker.module.css, src/components/DatePicker/DatePicker.tsx, src/components/Calendar/Calendar.module.css</files>
  <action>
Five CSS/layout fixes:

1. **DatePicker card sizing** (DatePicker.module.css): Change `.datePickerCard` from `p-sm` to `p-md` (or equivalent larger padding). Add `min-width: 280px` to ensure the card never collapses too small:
```css
.datePickerCard {
  padding: var(--spacing-md);
  min-width: 280px;
}
```

2. **Day cell sizing** (Calendar.module.css): Change `.dayCell` padding from `p-xxs` to `p-xs`. Add minimum dimensions:
```css
.dayCell {
  padding: var(--spacing-xs);
  min-width: 36px;
  min-height: 36px;
}
```

3. **Actions padding** (DatePicker.module.css): Add horizontal padding to `.actions`:
```css
.actions {
  padding: var(--spacing-xs) var(--spacing-sm);
}
```

4. **Trigger min-width** (DatePicker.tsx): Add `min-width: 160px` to the trigger button/element style so the date text has room to display. This may be an inline style or a CSS class addition.

5. **Calendar wrapper padding** (DatePicker.tsx): The Calendar component rendered inside the DatePicker card needs wrapper padding matching the Vue source: `px-sm pt-xxs`. Add a wrapping div around the Calendar component inside the card:
```tsx
<div className="px-sm pt-xxs">
  <Calendar ... />
</div>
```
Or if Calendar is already in a wrapper div, add these Tailwind classes to it.

Check the Vue source for exact spacing tokens used and match them. The goal is for the DatePicker to look professional with adequate whitespace, not cramped.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -n "min-width\|min-w\|p-md\|p-xs\|px-sm" src/components/DatePicker/DatePicker.module.css src/components/DatePicker/DatePicker.tsx src/components/Calendar/Calendar.module.css | head -10</automated>
  </verify>
  <done>DatePicker card has min-width 280px with p-md. Day cells are 36x36 min with p-xs. Actions have horizontal padding. Trigger has min-width. Calendar has wrapper padding.</done>
</task>

</tasks>

<verification>
- DatePicker story: card is visibly larger with proper whitespace
- Day cells are large enough to click comfortably
- Clear/Apply buttons properly sized at bottom
- Trigger shows date text without cramping
</verification>

<success_criteria>
- DatePicker proportions match Vue version's professional layout
- Day cells at least 36x36px
- Adequate whitespace throughout the component
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-30-SUMMARY.md`
</output>
