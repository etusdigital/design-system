---
phase: 06-composite-components
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Accordion/Accordion.mdx
  - src/components/Input/Input.stories.tsx
autonomous: true
requirements: [COMP-03, COMP-17]
gap_closure: true

must_haves:
  truths:
    - "Accordion.mdx does not exist on disk, preventing Storybook from loading a legacy MDX doc alongside the React story"
    - "Input.stories.tsx has clean git status (no unstaged changes)"
  artifacts:
    - path: "src/components/Accordion/Accordion.mdx"
      provides: "File must NOT exist"
  key_links:
    - from: ".storybook/main.ts"
      to: "src/**/*.mdx"
      via: "stories glob pattern"
      pattern: "src/\\*\\*/\\*.mdx"
---

<objective>
Delete the untracked Accordion.mdx file that was regenerated on disk after being deleted in commit 9844e21, and commit the unstaged Input.stories.tsx changes.

Purpose: The Storybook config includes `src/**/*.mdx` in its stories glob. The untracked Accordion.mdx imports from `./Accordion.stories.tsx` and will be loaded by Storybook, potentially causing rendering conflicts. Input.stories.tsx has local modifications (removed DomainType/UrlType stories) that need to be committed.
Output: Clean working tree for Phase 06 components with no legacy MDX artifacts.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-composite-components/06-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove untracked Accordion.mdx and commit Input.stories.tsx cleanup</name>
  <files>src/components/Accordion/Accordion.mdx, src/components/Input/Input.stories.tsx</files>
  <action>
1. Delete `src/components/Accordion/Accordion.mdx` from disk. This file was deleted in commit 9844e21 but re-appeared as an untracked file (likely regenerated by a pre-commit hook or filesystem artifact). It imports from `./Accordion.stories.tsx` and will be picked up by Storybook's `src/**/*.mdx` glob, causing a legacy MDX doc page to render alongside the React story.

2. Stage and commit `src/components/Input/Input.stories.tsx` which has unstaged changes removing the DomainType and UrlType story variants.

3. Add `Accordion.mdx` deletion to the same commit using `git add` on the deleted path (or `git rm` if tracked). Since it is untracked, simply deleting the file from disk is sufficient -- no git rm needed.

4. After committing, verify no other Phase 06 composite component `.mdx` files remain on disk by running: `find src/components/{Dialog,Drawer,Accordion,Tab,Pagination,Stepper,Select,AutoComplete,Dropdown,TagSelect,Filter,Carousel,RoundMenu,Calendar,DatePicker,Navbar,ColorPicker} -name "*.mdx" 2>/dev/null`

Note: Many `.mdx` files exist for components in other phases (Phase 2, 3, 7 etc.) -- those are NOT in scope. Only Phase 06 composite components should be clean.
  </action>
  <verify>
    <automated>test ! -f src/components/Accordion/Accordion.mdx && echo "Accordion.mdx deleted" && git diff --name-only src/components/Input/Input.stories.tsx | wc -l | grep -q 0 && echo "Input.stories.tsx clean"</automated>
  </verify>
  <done>Accordion.mdx no longer exists on disk. Input.stories.tsx changes are committed. No Phase 06 composite component directories contain .mdx files.</done>
</task>

</tasks>

<verification>
- `ls src/components/Accordion/Accordion.mdx` returns "No such file or directory"
- `git status` shows no unstaged changes for Phase 06 component files
- `find src/components/Accordion src/components/Drawer src/components/Tab src/components/Pagination src/components/Stepper src/components/Select src/components/AutoComplete src/components/Dropdown src/components/TagSelect src/components/Filter src/components/Carousel src/components/RoundMenu src/components/Calendar src/components/DatePicker src/components/Navbar src/components/ColorPicker -name "*.mdx" 2>/dev/null` returns empty
</verification>

<success_criteria>
- Accordion.mdx does not exist on disk
- Input.stories.tsx has no unstaged modifications
- All Phase 06 composite component directories are free of .mdx files
</success_criteria>

<output>
After completion, create `.planning/phases/06-composite-components/06-15-SUMMARY.md`
</output>
