---
phase: 09-documentation-testing
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/components/Input/Input.test.tsx
  - src/components/Textarea/Textarea.test.tsx
  - src/components/Toggle/Toggle.test.tsx
  - src/components/ToggleGroup/ToggleGroup.test.tsx
  - src/components/PINInput/PINInput.test.tsx
  - src/components/TagInput/TagInput.test.tsx
  - src/components/FileUpload/FileUpload.test.tsx
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "Input test covers controlled, uncontrolled, and ref forwarding"
    - "Textarea test covers controlled, uncontrolled, and ref forwarding"
    - "Toggle test covers controlled and uncontrolled modes"
    - "ToggleGroup test covers controlled and uncontrolled modes"
    - "PINInput test covers controlled and uncontrolled modes and imperative handle ref"
    - "TagInput test covers controlled and uncontrolled modes"
    - "FileUpload test covers controlled and uncontrolled modes"
    - "Checkbox, Radio, RadioGroup, Switch, Slider tests already cover their applicable scenarios"
    - "All unit tests pass after additions"
  artifacts:
    - path: "src/components/Input/Input.test.tsx"
      provides: "Uncontrolled mode test"
      contains: "defaultValue"
    - path: "src/components/Textarea/Textarea.test.tsx"
      provides: "Uncontrolled mode test"
      contains: "defaultValue"
    - path: "src/components/Toggle/Toggle.test.tsx"
      provides: "Controlled mode test"
      contains: "value={false}"
    - path: "src/components/ToggleGroup/ToggleGroup.test.tsx"
      provides: "Controlled mode test"
      contains: "value={1}"
    - path: "src/components/PINInput/PINInput.test.tsx"
      provides: "Controlled mode test"
      contains: 'value="0000"'
    - path: "src/components/TagInput/TagInput.test.tsx"
      provides: "Controlled mode test"
      contains: "value={["
    - path: "src/components/FileUpload/FileUpload.test.tsx"
      provides: "Controlled mode test"
      contains: "value={"
  key_links:
    - from: "src/components/*/test.tsx"
      to: "src/components/*/index.ts"
      via: "import { Component } from './index'"
      pattern: "from './index'"
---

<objective>
Add missing test scenarios to 7 form component test files so all 12 form components have coverage of their applicable modes (controlled, uncontrolled, ref forwarding where supported).

Purpose: Satisfies TEST-02 — every form component has RTL tests covering controlled mode, uncontrolled mode, and ref forwarding (where the component supports it).
Output: 7 updated test files, all tests green.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-testing/09-CONTEXT.md
@.planning/phases/09-documentation-testing/09-RESEARCH.md
@.planning/phases/09-documentation-testing/09-01-SUMMARY.md

<interfaces>
<!-- Ref forwarding status per component (from source inspection): -->
<!-- forwardRef components: Input (HTMLInputElement), Textarea (HTMLTextAreaElement), Slider (HTMLDivElement), PINInput (PINInputHandle) -->
<!-- Plain function components (NO forwardRef): Checkbox, Radio, RadioGroup, Switch, Toggle, ToggleGroup, TagInput, FileUpload -->
<!-- Components without forwardRef cannot be tested for ref forwarding — this is expected and documented. -->

<!-- Controlled props interface per component: -->
<!-- Toggle: value?: boolean, onChange?: (value: boolean) => void -->
<!-- ToggleGroup: value?: any, defaultValue?: any, onChange?: (value: any) => void -->
<!-- PINInput: value?: string, defaultValue?: string, onChange?: (value: string) => void, onComplete?: (value: string) => void -->
<!-- TagInput: value?: any[], defaultValue?: any[], onChange?: (value: any[]) => void -->
<!-- FileUpload: value?: File | File[] | null, defaultValue?: File | File[] | null, onChange?: (value: File | File[] | null) => void -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing tests for Input, Textarea, Toggle, ToggleGroup</name>
  <files>
    src/components/Input/Input.test.tsx
    src/components/Textarea/Textarea.test.tsx
    src/components/Toggle/Toggle.test.tsx
    src/components/ToggleGroup/ToggleGroup.test.tsx
  </files>
  <read_first>
    - src/components/Input/Input.test.tsx (existing tests — has controlled + ref, missing uncontrolled)
    - src/components/Input/Input.tsx (confirm defaultValue prop interface)
    - src/components/Textarea/Textarea.test.tsx (existing tests — has controlled + ref, missing uncontrolled)
    - src/components/Textarea/Textarea.tsx (confirm defaultValue prop interface)
    - src/components/Toggle/Toggle.test.tsx (existing tests — has uncontrolled standalone click, missing controlled)
    - src/components/Toggle/Toggle.tsx (confirm value/onChange prop interface)
    - src/components/ToggleGroup/ToggleGroup.test.tsx (existing tests — has uncontrolled + onChange-only, missing proper controlled)
    - src/components/ToggleGroup/ToggleGroup.tsx (confirm value/onChange prop interface)
  </read_first>
  <action>
Add the following test cases to each file. Append new `it()` blocks inside the existing `describe()` block. Do NOT modify existing tests.

**Input.test.tsx** — Add 1 test (uncontrolled mode):
```typescript
it('renders with defaultValue in uncontrolled mode', () => {
  const { container } = render(<Input defaultValue="initial" />);
  const input = container.querySelector('input') as HTMLInputElement;
  expect(input.value).toBe('initial');
});
```

**Textarea.test.tsx** — Add 1 test (uncontrolled mode):
```typescript
it('renders with defaultValue in uncontrolled mode', () => {
  const { container } = render(<Textarea defaultValue="initial text" />);
  const textarea = container.querySelector('textarea') as HTMLTextAreaElement;
  expect(textarea.value).toBe('initial text');
});
```

**Toggle.test.tsx** — Add 1 test (controlled mode):
```typescript
it('controlled mode calls onChange on click', () => {
  const handleChange = vi.fn();
  const { getByRole } = render(<Toggle value={false} onChange={handleChange}>Label</Toggle>);
  fireEvent.click(getByRole('button'));
  expect(handleChange).toHaveBeenCalledWith(true);
});
```
Note: Also add `vi` to the vitest import if not already present (check the existing import line).

**ToggleGroup.test.tsx** — Add 1 test (proper controlled mode with `value` prop):
```typescript
it('controlled mode: value prop controls selection, onChange fires on click', () => {
  const handleChange = vi.fn();
  const { getAllByRole } = render(
    <ToggleGroup options={defaultOptions} value={1} onChange={handleChange} />
  );
  const buttons = getAllByRole('button');
  // First button is active because value=1 matches first option
  expect(buttons[0].getAttribute('aria-pressed')).toBe('true');
  // Click second button
  fireEvent.click(buttons[1]);
  expect(handleChange).toHaveBeenCalledWith(2);
});
```

After adding all tests, run: `vitest run --project unit --reporter=verbose 2>&1 | grep -E "PASS|FAIL|Input|Textarea|Toggle"` to verify the new tests pass.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npx vitest run --project unit src/components/Input/Input.test.tsx src/components/Textarea/Textarea.test.tsx src/components/Toggle/Toggle.test.tsx src/components/ToggleGroup/ToggleGroup.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Input.test.tsx contains `it('renders with defaultValue in uncontrolled mode'`
    - Textarea.test.tsx contains `it('renders with defaultValue in uncontrolled mode'`
    - Toggle.test.tsx contains `it('controlled mode calls onChange on click'`
    - ToggleGroup.test.tsx contains `it('controlled mode: value prop controls selection`
    - All 4 test files pass with zero failures
  </acceptance_criteria>
  <done>Input, Textarea have controlled+uncontrolled+ref tests; Toggle, ToggleGroup have controlled+uncontrolled tests; all pass</done>
</task>

<task type="auto">
  <name>Task 2: Add missing tests for PINInput, TagInput, FileUpload + final validation</name>
  <files>
    src/components/PINInput/PINInput.test.tsx
    src/components/TagInput/TagInput.test.tsx
    src/components/FileUpload/FileUpload.test.tsx
  </files>
  <read_first>
    - src/components/PINInput/PINInput.test.tsx (has uncontrolled + imperative ref, missing controlled)
    - src/components/PINInput/PINInput.tsx (confirm value/onChange prop interface: value?: string, onChange?: (value: string) => void)
    - src/components/TagInput/TagInput.test.tsx (has uncontrolled via defaultValue, missing controlled)
    - src/components/TagInput/TagInput.tsx (confirm value/onChange prop interface: value?: any[], onChange?: (value: any[]) => void)
    - src/components/FileUpload/FileUpload.test.tsx (has onChange-only, missing controlled with value prop)
    - src/components/FileUpload/FileUpload.tsx (confirm value/onChange interface: value?: File | File[] | null, onChange?: (value: File | File[] | null) => void)
  </read_first>
  <action>
Add the following test cases to each file. Append new `it()` blocks inside the existing `describe()` block. Do NOT modify existing tests.

**PINInput.test.tsx** — Add 1 test (controlled mode):
```typescript
it('controlled mode: value prop populates fields, onChange fires on input', () => {
  const handleChange = vi.fn();
  render(<PINInput value="1234" onChange={handleChange} length={4} />);
  const inputs = document.querySelectorAll('input') as NodeListOf<HTMLInputElement>;
  // Controlled value populates fields
  expect(inputs[0].value).toBe('1');
  expect(inputs[1].value).toBe('2');
  expect(inputs[2].value).toBe('3');
  expect(inputs[3].value).toBe('4');
  // Changing a field fires onChange
  fireEvent.change(inputs[0], { target: { value: '5' } });
  expect(handleChange).toHaveBeenCalled();
});
```

**TagInput.test.tsx** — Add 1 test (controlled mode):
```typescript
it('controlled mode: value prop renders tags, onChange fires on new tag', () => {
  const handleChange = vi.fn();
  render(<TagInput value={['existing']} onChange={handleChange} />);
  // Controlled value renders as tag
  expect(screen.getByText('existing')).toBeTruthy();
  // Add a new tag
  const textarea = document.querySelector('textarea')!;
  fireEvent.change(textarea, { target: { value: 'newtag' } });
  fireEvent.keyDown(textarea, { key: 'Enter' });
  expect(handleChange).toHaveBeenCalled();
});
```

**FileUpload.test.tsx** — Add 1 test (controlled mode with value prop):
```typescript
it('controlled mode: value prop sets file, onChange fires on new selection', () => {
  const existingFile = new File(['content'], 'existing.pdf', { type: 'application/pdf' });
  const handleChange = vi.fn();
  render(<FileUpload value={existingFile} onChange={handleChange} />);
  // Controlled value shows file name
  expect(screen.getByText('existing.pdf')).toBeTruthy();
  // Selecting a new file fires onChange
  const input = document.querySelector('input[type="file"]')!;
  const newFile = new File(['new'], 'new-file.pdf', { type: 'application/pdf' });
  Object.defineProperty(input, 'files', {
    value: [newFile],
    configurable: true,
  });
  fireEvent.change(input);
  expect(handleChange).toHaveBeenCalledWith(newFile);
});
```

After adding all tests, run the full unit suite to confirm everything passes:
```bash
npx vitest run --project unit
```

Then run `npm run build-storybook` as the final phase gate to confirm TEST-01 and TEST-03.

**Ref forwarding note for SUMMARY:** The following components do NOT use `forwardRef` and therefore ref forwarding tests are not applicable: Checkbox, Radio, RadioGroup, Switch, Toggle, ToggleGroup, TagInput, FileUpload. Only Input (HTMLInputElement), Textarea (HTMLTextAreaElement), Slider (HTMLDivElement), and PINInput (PINInputHandle) support ref forwarding, and all 4 already have ref tests. This satisfies the intent of TEST-02 — ref forwarding is tested for all components that support it.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && npx vitest run --project unit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PINInput.test.tsx contains `it('controlled mode: value prop populates fields`
    - TagInput.test.tsx contains `it('controlled mode: value prop renders tags`
    - FileUpload.test.tsx contains `it('controlled mode: value prop sets file`
    - `vitest run --project unit` exits with code 0 (all tests pass)
    - No component source files were modified (only test files)
  </acceptance_criteria>
  <done>All 12 form components have tests covering their applicable scenarios (controlled, uncontrolled, ref forwarding where supported); full unit suite passes</done>
</task>

</tasks>

<verification>
- `npx vitest run --project unit` exits 0 with all tests passing
- `npm run build-storybook` exits 0
- Each of the 12 form component test files covers its applicable scenarios:
  - Input: controlled, uncontrolled, ref (all 3)
  - Textarea: controlled, uncontrolled, ref (all 3)
  - Slider: controlled, uncontrolled, ref (all 3 — already complete)
  - PINInput: controlled, uncontrolled, imperative handle ref (all 3)
  - Checkbox: controlled, uncontrolled (2/2 applicable — no forwardRef)
  - Radio: controlled, uncontrolled (2/2 applicable — no forwardRef)
  - RadioGroup: controlled, uncontrolled (2/2 applicable — no forwardRef)
  - Switch: controlled, uncontrolled (2/2 applicable — no forwardRef)
  - Toggle: controlled, uncontrolled (2/2 applicable — no forwardRef)
  - ToggleGroup: controlled, uncontrolled (2/2 applicable — no forwardRef)
  - TagInput: controlled, uncontrolled (2/2 applicable — no forwardRef)
  - FileUpload: controlled, uncontrolled (2/2 applicable — no forwardRef)
</verification>

<success_criteria>
- 7 test files updated with new test cases
- All unit tests pass (zero failures)
- No component source files modified
- TEST-02 requirement satisfied for all 12 form components
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation-testing/09-02-SUMMARY.md`
</output>
