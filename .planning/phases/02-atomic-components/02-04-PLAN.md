---
phase: 02-atomic-components
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/FloatCard/FloatCard.tsx
  - src/components/FloatCard/FloatCard.module.css
  - src/components/FloatCard/FloatCard.stories.tsx
  - src/components/Alert/Alert.tsx
  - src/components/Alert/Alert.module.css
  - src/components/Alert/Alert.stories.tsx
  - src/components/Connector/Connector.tsx
  - src/components/Connector/Connector.module.css
  - src/components/Connector/Connector.stories.tsx
autonomous: true
requirements: [ATOM-10, ATOM-17, ATOM-19]

must_haves:
  truths:
    - "FloatCard opens/closes a card panel via createPortal into document.body"
    - "FloatCard positions itself relative to trigger using getBoundingClientRect"
    - "FloatCard fades in/out using useTransition hook"
    - "FloatCard closes on click-outside and scroll"
    - "Alert renders with icon, type variants, and expand/collapse behavior"
    - "Alert.Actions compound sub-component renders named slot content"
    - "Connector renders a flex container grouping child elements"
  artifacts:
    - path: "src/components/FloatCard/FloatCard.tsx"
      provides: "FloatCard portal component"
      exports: ["FloatCard", "FloatCardProps"]
    - path: "src/components/Alert/Alert.tsx"
      provides: "Alert component with expand/collapse"
      exports: ["Alert", "AlertProps"]
    - path: "src/components/Connector/Connector.tsx"
      provides: "Connector flex grouping component"
      exports: ["Connector", "ConnectorProps"]
  key_links:
    - from: "src/components/FloatCard/FloatCard.tsx"
      to: "src/hooks/useTransition.ts"
      via: "import { useTransition }"
      pattern: "useTransition"
    - from: "src/components/FloatCard/FloatCard.tsx"
      to: "src/components/Card/Card.tsx"
      via: "import { Card }"
      pattern: "import.*Card"
    - from: "src/components/Alert/Alert.tsx"
      to: "src/components/Icon/Icon.tsx"
      via: "import { Icon }"
      pattern: "import.*Icon"
---

<objective>
Migrate FloatCard (portal + positioning, similar to Tooltip), Alert (ResizeObserver + expand/collapse + named slot), and Connector (inline Group utility wrapper).

Purpose: FloatCard is a dependency for Breadcrumb. Alert demonstrates the ResizeObserver and compound sub-component patterns. Connector wraps an internal utility (Group) implemented inline.
Output: 3 working React components with stories, Vue files deleted.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-atomic-components/02-CONTEXT.md
@.planning/phases/02-atomic-components/02-RESEARCH.md

Source Vue files:
@src/components/FloatCard/FloatCard.vue
@src/components/Alert/Alert.vue
@src/components/Connector/Connector.vue

Existing stories:
@src/components/FloatCard/FloatCard.stories.ts
@src/components/Alert/Alert.stories.ts
@src/components/Connector/Connector.stories.ts

Dependencies:
@src/components/Card/Card.tsx
@src/components/Icon/Icon.tsx
@src/hooks/useTransition.ts
@src/hooks/useControllable.ts

Internal utility referenced by Connector:
@src/utils/components/Group.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate FloatCard with portal positioning</name>
  <files>
    src/components/FloatCard/FloatCard.tsx
    src/components/FloatCard/FloatCard.module.css
    src/components/FloatCard/FloatCard.stories.tsx
  </files>
  <action>
Read `FloatCard.vue` — extract props, template, positioning logic, click/wheel handlers, CSS.

Create `FloatCard.tsx`:
1. **Props interface** matching Vue: `children`, `className`, plus any trigger/control props from Vue source. FloatCard likely uses `useControllable` for open/close state (Vue uses `useOptionalModel`).
2. **Open/close state:** Use `useControllable` if the Vue version supports controlled mode (modelValue), otherwise `useState(false)`.
3. **Transition:** `useTransition(isOpen, { duration: ... })` for fade.
4. **Portal:** `createPortal(<card-element>, document.body)` when `isMounted`.
5. **Positioning:** Same pattern as Tooltip — `useEffect` with `getBoundingClientRect()` on trigger element, set `style.left/top` on portal element. Port the exact positioning logic from Vue source (may differ from Tooltip in alignment strategy).
6. **Click-outside close:** `useEffect` with `document.addEventListener('click', handler)` — check if click target is outside the FloatCard and trigger elements. Cleanup in return.
7. **Scroll close:** `document.addEventListener('wheel', closeHandler)` with cleanup.
8. **Import Card** from `../Card` if FloatCard wraps Card visually.

Create `FloatCard.module.css` — extract styles from Vue `<style scoped>`. Same transition pattern as Tooltip (opacity 0 → 1 with `.active` class).

Create `FloatCard.stories.tsx` — adapt Vue story. Delete Vue files.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>FloatCard opens/closes via portal, positions correctly, closes on click-outside and scroll, fades in/out. Vue files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Alert and Connector to React TSX</name>
  <files>
    src/components/Alert/Alert.tsx
    src/components/Alert/Alert.module.css
    src/components/Alert/Alert.stories.tsx
    src/components/Connector/Connector.tsx
    src/components/Connector/Connector.module.css
    src/components/Connector/Connector.stories.tsx
  </files>
  <action>
**Alert migration:**
1. Read `Alert.vue` — extract props, template, ResizeObserver logic, named slot usage, CSS.
2. Create `Alert.tsx`:
   - Props: `type` (success/warning/danger/info), `icon`, `closeable`, `children`, `className`, plus any others from Vue source.
   - **Named slot "actions"** → `Alert.Actions` compound sub-component per locked decision.
   - **Expand/collapse state:** `useState(false)` for expanded. Show/hide content based on height comparison.
   - **ResizeObserver lifecycle** (Pattern 8 from RESEARCH.md):
     - `useEffect` with `new ResizeObserver(resize)` — observe card and content refs.
     - Cleanup: `observer.disconnect()` in return function.
     - `resize` function compares content height to card height, toggles expandable state.
   - **Re-run resize on render:** `useEffect(() => { resize(); })` with no deps (replaces Vue `onUpdated`).
   - Import `{ Icon }` from `../Icon` for the alert icon.
3. Create `Alert.module.css` — extract styles, handle expand/collapse transition.
4. Create `Alert.stories.tsx`, delete Vue files.

**Connector migration:**
1. Read `Connector.vue` — it wraps `Group.vue` utility component.
2. Read `src/utils/components/Group.vue` — extract the ~10-line flex container logic.
3. Create `Connector.tsx`:
   - Implement Group utility inline (it is ~10 lines: a flex container with gap/direction props).
   - Do NOT import from `src/utils/components/` — Phase 4 will properly migrate those.
   - Props: whatever Connector.vue passes through to Group, plus `children`, `className`.
4. Create `Connector.module.css` if needed, `Connector.stories.tsx`, delete Vue files.
  </action>
  <verify>
    <automated>npx storybook build --quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Alert renders with type variants, icon, expand/collapse, and Actions sub-component. Connector renders as flex grouping container. Vue files deleted.</done>
</task>

</tasks>

<verification>
- `npx storybook build --quiet` completes without errors
- FloatCard, Alert, Connector importable from `src/components`
- No `.vue` or `.stories.ts` (Vue) files remain in these directories
- Alert.Actions compound sub-component works
- FloatCard portal renders into document.body
</verification>

<success_criteria>
- FloatCard opens/closes with transition, positions via portal, closes on outside click/scroll
- Alert renders all type variants, expands/collapses long content, shows Actions slot
- Connector renders as flex container (Group utility inlined)
- All 3 Storybook stories render without errors
- 6 Vue files deleted
</success_criteria>

<output>
After completion, create `.planning/phases/02-atomic-components/02-04-SUMMARY.md`
</output>
