---
phase: 02-atomic-components
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Badge/Badge.tsx
  - src/components/Badge/Badge.module.css
  - src/components/Badge/colors.module.css
  - src/components/Badge/Badge.stories.tsx
  - src/components/StatusBadge/StatusBadge.tsx
  - src/components/StatusBadge/StatusBadge.module.css
  - src/components/StatusBadge/StatusBadge.stories.tsx
  - src/components/Button/Button.tsx
  - src/components/Button/Button.module.css
  - src/components/Button/colors.css
  - src/components/Button/Button.stories.tsx
autonomous: true
requirements: [ATOM-01, ATOM-03, ATOM-04]

must_haves:
  truths:
    - "Badge renders with same color, size, type variants as Vue version"
    - "Badge shows Spinner when loading=true"
    - "Badge custom color uses blendColors utility for background"
    - "StatusBadge wraps Badge with status-specific styling"
    - "Button renders with same color, variant, size props as Vue version"
    - "Button shows Spinner when loading=true"
    - "Button hover state applies blendColors for custom backgrounds"
  artifacts:
    - path: "src/components/Badge/Badge.tsx"
      provides: "Badge component with color variants"
      exports: ["Badge", "BadgeProps"]
    - path: "src/components/StatusBadge/StatusBadge.tsx"
      provides: "StatusBadge wrapper component"
      exports: ["StatusBadge", "StatusBadgeProps"]
    - path: "src/components/Button/Button.tsx"
      provides: "Button component with all variants"
      exports: ["Button", "ButtonProps"]
  key_links:
    - from: "src/components/Badge/Badge.tsx"
      to: "src/components/Spinner/Spinner.tsx"
      via: "import { Spinner }"
      pattern: "import.*Spinner"
    - from: "src/components/Button/Button.tsx"
      to: "src/components/Spinner/Spinner.tsx"
      via: "import { Spinner }"
      pattern: "import.*Spinner"
    - from: "src/components/Badge/Badge.tsx"
      to: "src/utils/index.ts"
      via: "import { blendColors }"
      pattern: "blendColors"
---

<objective>
Migrate Badge, StatusBadge, and Button — three core interactive components that depend on Spinner (from Plan 01) and use the blendColors utility for custom color backgrounds.

Purpose: Badge and Button are the most commonly used atomic components. Button is also a dependency for Image and Profile in later plans.
Output: 3 working React components with stories, Vue files deleted.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-atomic-components/02-CONTEXT.md
@.planning/phases/02-atomic-components/02-RESEARCH.md

Source Vue files:
@src/components/Badge/Badge.vue
@src/components/StatusBadge/StatusBadge.vue
@src/components/Button/Button.vue

Existing stories:
@src/components/Badge/Badge.stories.ts
@src/components/StatusBadge/StatusBadge.stories.ts
@src/components/Button/Button.stories.ts

Dependencies (available from Plan 01 or Phase 1):
@src/components/Spinner/Spinner.tsx
@src/components/Icon/Icon.tsx
@src/utils/index.ts

Existing CSS (may need migration to modules):
@src/components/Button/colors.css
@src/components/Badge/colors.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Badge and StatusBadge to React TSX</name>
  <files>
    src/components/Badge/Badge.tsx
    src/components/Badge/Badge.module.css
    src/components/Badge/colors.module.css
    src/components/Badge/Badge.stories.tsx
    src/components/StatusBadge/StatusBadge.tsx
    src/components/StatusBadge/StatusBadge.module.css
    src/components/StatusBadge/StatusBadge.stories.tsx
  </files>
  <action>
**Badge migration:**
1. Read `Badge.vue` — extract full props, template, styles, slot usage.
2. Create `Badge.tsx` with `BadgeProps` interface matching all Vue props:
   - `labelValue`, `color`, `size` ('small'|'medium'|'large'), `type` ('default'|'secondary'|'heavy'), `loading`, `closeable`, `icon`, `isAppendedIcon`, `children`, `className`, `onClose`.
   - Default values via destructuring defaults.
   - Import `{ Spinner }` from `../Spinner` — render when `loading=true`.
   - Import `{ Icon }` from `../Icon` — render when `icon` is set.
   - Import `{ blendColors }` from `../../utils` — compute `background` style when custom `color` is provided.
   - Vue `v-bind(color)` in CSS → `style={{ color, borderColor: color, background: blendColors(color) }}` inline.
   - Guard `blendColors` call: only call when `color` is truthy and `typeof document !== 'undefined'`.
   - CSS Module classes via `clsx(styles.badge, styles[size], styles[type])`.
   - Child Spinner: pass `className={styles.badgeSpinner}` to avoid CSS Module descendant selector issue (Pitfall 1).
3. Create `Badge.module.css` and `colors.module.css`:
   - Extract layout/structure styles from Vue `<style scoped>` into `Badge.module.css`.
   - Extract color variant classes into `colors.module.css`.
   - Use `:global(.spinner)` or pass className for child component targeting.
4. Create `Badge.stories.tsx` — adapt from Vue story, CSF3 format.
5. Delete `Badge.vue` and `Badge.stories.ts`.

**StatusBadge migration:**
1. Read `StatusBadge.vue` — thin wrapper over Badge.
2. Create `StatusBadge.tsx`: Import Badge, pass through props with status-specific defaults/styling.
3. Create `StatusBadge.module.css` if needed.
4. Create `StatusBadge.stories.tsx`, delete Vue files.
  </action>
  <verify>
    <automated>npx storybook build --quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Badge renders with all color/size/type variants, loading spinner, closeable X, and custom color backgrounds. StatusBadge renders with status styling. Vue files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Button to React TSX</name>
  <files>
    src/components/Button/Button.tsx
    src/components/Button/Button.module.css
    src/components/Button/Button.stories.tsx
  </files>
  <action>
1. Read `Button.vue` — extract full props, template, computed styles, hover logic, CSS.
2. Create `Button.tsx` with `ButtonProps` interface:
   - Props: `type` ('button'|'reset'|'submit'), `color`, `variant` ('default'|'secondary'|'plain'|'reverse'), `size` ('small'|'medium'|'large'), `disabled`, `round`, `alwaysOpen`, `loading`, `progress`, `icon`, `appendIcon`, `children`, `className`, `onClick`, plus any other props from Vue source.
   - **Hover state:** `const [isHovering, setIsHovering] = useState(false)` + `onMouseEnter`/`onMouseLeave` (Pattern 3 from RESEARCH.md).
   - **Custom background:** When custom `color` prop is set, use `blendColors(color)` to compute hover background. Guard with `typeof document !== 'undefined'` (Pitfall 5).
   - **Spinner dependency:** Import and render `<Spinner className={styles.buttonSpinner} />` when `loading=true`.
   - **Icon dependency:** Import and render `<Icon>` for `icon` and `appendIcon` props.
   - **Progress bar:** If `progress` prop is set (0-1 range), render progress overlay element.
   - **Note:** Keep `colors.css` as a non-module CSS file if it uses Tailwind `@apply` extensively and is already structured as global color utilities. Only convert to CSS Modules if scoping is needed. Check existing `colors.css` content first.
3. Create `Button.module.css`:
   - Extract layout/structure styles from Vue `<style scoped>`.
   - Handle child component targeting via `:global()` or className prop passthrough.
4. Create `Button.stories.tsx` — adapt from Vue story with all argTypes.
5. Delete `Button.vue` and `Button.stories.ts`.
  </action>
  <verify>
    <automated>npx storybook build --quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Button renders with all color/variant/size combinations, hover effects, loading spinner, icon support, and progress overlay. Vue files deleted.</done>
</task>

</tasks>

<verification>
- `npx storybook build --quiet` completes without errors
- Badge, StatusBadge, Button importable from `src/components`
- No `.vue` or `.stories.ts` (Vue) files remain in Badge, StatusBadge, or Button directories
- `npx tsc --noEmit` passes with no new errors
</verification>

<success_criteria>
- Badge renders correctly with all 3 sizes, 3 types, loading state, closeable state, custom colors
- StatusBadge wraps Badge with status-specific styling
- Button renders correctly with all 6 colors, 4 variants, 3 sizes, hover effects, loading, progress
- blendColors works correctly for custom color backgrounds
- Spinner renders as child of Badge and Button when loading=true
- All 3 Storybook stories render without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-atomic-components/02-03-SUMMARY.md`
</output>
