---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/index.ts
  - src/index.ts
  - src/components/Accordion/index.ts
  - src/components/ActionCard/index.ts
  - src/components/Alert/index.ts
  - src/components/AutoComplete/index.ts
  - src/components/Avatar/index.ts
  - src/components/Badge/index.ts
  - src/components/Breadcrumb/index.ts
  - src/components/Button/index.ts
  - src/components/Calendar/index.ts
  - src/components/Card/index.ts
  - src/components/Carousel/index.ts
  - src/components/Checkbox/index.ts
  - src/components/ColorPicker/index.ts
  - src/components/Confirm/index.ts
  - src/components/Connector/index.ts
  - src/components/Crop/index.ts
  - src/components/DatePicker/index.ts
  - src/components/Dialog/index.ts
  - src/components/Drawer/index.ts
  - src/components/Dropdown/index.ts
  - src/components/FileUpload/index.ts
  - src/components/Filter/index.ts
  - src/components/FloatCard/index.ts
  - src/components/History/index.ts
  - src/components/Icon/index.ts
  - src/components/IconCard/index.ts
  - src/components/Image/index.ts
  - src/components/Input/index.ts
  - src/components/MetricCard/index.ts
  - src/components/Navbar/index.ts
  - src/components/Pagination/index.ts
  - src/components/PINInput/index.ts
  - src/components/Profile/index.ts
  - src/components/ProgressBar/index.ts
  - src/components/Radio/index.ts
  - src/components/RadioGroup/index.ts
  - src/components/RichTextEditor/index.ts
  - src/components/RoundMenu/index.ts
  - src/components/Select/index.ts
  - src/components/Separator/index.ts
  - src/components/Sidebar/index.ts
  - src/components/Skeleton/index.ts
  - src/components/Slider/index.ts
  - src/components/Spinner/index.ts
  - src/components/StatusBadge/index.ts
  - src/components/Stepper/index.ts
  - src/components/Switch/index.ts
  - src/components/Tab/index.ts
  - src/components/Table/index.ts
  - src/components/TagInput/index.ts
  - src/components/TagSelect/index.ts
  - src/components/Textarea/index.ts
  - src/components/Toast/index.ts
  - src/components/Toggle/index.ts
  - src/components/ToggleGroup/index.ts
  - src/components/Tooltip/index.ts
  - src/components/Tree/index.ts
autonomous: true
requirements:
  - FOUND-07
  - FOUND-12

must_haves:
  truths:
    - "All 57 component index.ts files export named React components without Vue Plugin wrappers"
    - "src/components/index.ts exports all 57 components as named exports"
    - "src/index.ts re-exports all components and hooks without Vue plugin install"
  artifacts:
    - path: "src/components/Button/index.ts"
      provides: "Example of converted index.ts pattern"
      contains: "export"
    - path: "src/components/index.ts"
      provides: "Barrel file for all components"
      contains: "export"
    - path: "src/index.ts"
      provides: "Main package entry point with all named exports"
      contains: "export"
  key_links:
    - from: "src/index.ts"
      to: "src/components/index.ts"
      via: "re-export all components"
      pattern: "export.*from.*components"
    - from: "src/components/index.ts"
      to: "src/components/*/index.ts"
      via: "barrel re-exports"
      pattern: "export.*from"
---

<objective>
Convert all 57 component index.ts files from Vue Plugin exports to plain named exports, and rewrite the main entry point.

Purpose: Remove all Vue-specific plugin wrappers so the package exports clean React components. This is a bulk mechanical transformation with a single repeatable pattern.
Output: 57 updated index.ts files, updated barrel file, rewritten main entry.
</objective>

<execution_context>
@/Users/etus_0032/.claude/get-shit-done/workflows/execute-plan.md
@/Users/etus_0032/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Current patterns to convert -->

Current component index.ts pattern (all 57 follow this):
```typescript
import type { App, Plugin } from 'vue';
import Button from './Button.vue';

export default {
    install(Vue: App) {
        Vue.component('Button', Button);
    },
} as Plugin;

export { Button }
```

Target React pattern:
```typescript
export { Button } from './Button';
```
Note: The .tsx component files don't exist yet — they'll be created in Phases 2-7. For now, the index.ts files should use a STUB pattern that exports a placeholder React component so TypeScript doesn't error. Each stub should be a simple functional component that renders a div with the component name.

Current src/components/index.ts barrel:
```typescript
export { default as Accordion } from './Accordion';
export { default as ActionCard } from './ActionCard';
// ... 57 entries, all `export { default as X }`
```

Current src/index.ts:
```typescript
import type { App } from 'vue';
import * as components from './components';
// ... Vue plugin install function
export default { install(app: App) { ... } };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all 57 component index.ts files to React named exports with stub components</name>
  <files>src/components/*/index.ts</files>
  <action>
    For each of the 57 component directories, rewrite `index.ts` to remove the Vue Plugin wrapper and create a stub TSX component file.

    **Pattern for each component (e.g., Button):**

    1. Create a stub `src/components/Button/Button.tsx`:
       ```tsx
       // TODO: Migrate from Button.vue in Phase 2+
       export function Button(props: Record<string, unknown>) {
         return <div data-component="Button" {...props} />;
       }
       ```

    2. Rewrite `src/components/Button/index.ts`:
       ```typescript
       export { Button } from './Button';
       ```

    **Apply this pattern to ALL 57 components.** The component names are:
    Accordion, ActionCard, Alert, AutoComplete, Avatar, Badge, Breadcrumb, Button, Calendar, Card, Carousel, Checkbox, ColorPicker, Confirm, Connector, Crop, DatePicker, Dialog, Drawer, Dropdown, FileUpload, Filter, FloatCard, History, Icon, IconCard, Image, Input, MetricCard, Navbar, Pagination, PINInput, Profile, ProgressBar, Radio, RadioGroup, RichTextEditor, RoundMenu, Select, Separator, Sidebar, Skeleton, Slider, Spinner, StatusBadge, Stepper, Switch, Tab, Table, TagInput, TagSelect, Textarea, Toast, Toggle, ToggleGroup, Tooltip, Tree

    **Important:**
    - Do NOT delete the .vue files — they serve as reference for actual migration in Phases 2-7
    - Each stub TSX uses a simple `function ComponentName` (not arrow function) so the display name is automatically set
    - Each stub accepts `Record<string, unknown>` props to avoid type errors from any consumer
    - The `data-component` attribute helps identify stubs during testing
    - Keep the existing `colors.css` and other files in each directory untouched

    Use a scripted approach: write a small Node.js script or use shell loop to generate all 57 stubs + index.ts files efficiently. Do not write each one by hand.
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && TOTAL=$(ls -d src/components/*/index.ts | wc -l | tr -d ' ') && VUE_COUNT=$(grep -rl "Plugin" src/components/*/index.ts 2>/dev/null | wc -l | tr -d ' ') && TSX_COUNT=$(ls src/components/*/*.tsx 2>/dev/null | wc -l | tr -d ' ') && echo "Total: $TOTAL, Vue remaining: $VUE_COUNT, TSX stubs: $TSX_COUNT" && test "$VUE_COUNT" -eq 0 && test "$TSX_COUNT" -ge 57 && echo "OK"</automated>
  </verify>
  <done>All 57 component index.ts files export named React components (no Vue Plugin/install). Each has a stub .tsx file. No .vue files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite barrel file and main entry point</name>
  <files>src/components/index.ts, src/index.ts</files>
  <action>
    1. Rewrite `src/components/index.ts` — change from `export { default as X }` to named re-exports:
       ```typescript
       export { Accordion } from './Accordion';
       export { ActionCard } from './ActionCard';
       export { Alert } from './Alert';
       export { AutoComplete } from './AutoComplete';
       export { Avatar } from './Avatar';
       export { Badge } from './Badge';
       export { Breadcrumb } from './Breadcrumb';
       export { Button } from './Button';
       export { Calendar } from './Calendar';
       export { Card } from './Card';
       export { Carousel } from './Carousel';
       export { Checkbox } from './Checkbox';
       export { ColorPicker } from './ColorPicker';
       export { Confirm } from './Confirm';
       export { Connector } from './Connector';
       export { Crop } from './Crop';
       export { DatePicker } from './DatePicker';
       export { Dialog } from './Dialog';
       export { Drawer } from './Drawer';
       export { Dropdown } from './Dropdown';
       export { FileUpload } from './FileUpload';
       export { Filter } from './Filter';
       export { FloatCard } from './FloatCard';
       export { History } from './History';
       export { Icon } from './Icon';
       export { IconCard } from './IconCard';
       export { Image } from './Image';
       export { Input } from './Input';
       export { MetricCard } from './MetricCard';
       export { Navbar } from './Navbar';
       export { Pagination } from './Pagination';
       export { PINInput } from './PINInput';
       export { Profile } from './Profile';
       export { ProgressBar } from './ProgressBar';
       export { Radio } from './Radio';
       export { RadioGroup } from './RadioGroup';
       export { RichTextEditor } from './RichTextEditor';
       export { RoundMenu } from './RoundMenu';
       export { Select } from './Select';
       export { Separator } from './Separator';
       export { Sidebar } from './Sidebar';
       export { Skeleton } from './Skeleton';
       export { Slider } from './Slider';
       export { Spinner } from './Spinner';
       export { StatusBadge } from './StatusBadge';
       export { Stepper } from './Stepper';
       export { Switch } from './Switch';
       export { Tab } from './Tab';
       export { Table } from './Table';
       export { TagInput } from './TagInput';
       export { TagSelect } from './TagSelect';
       export { Textarea } from './Textarea';
       export { Toast } from './Toast';
       export { Toggle } from './Toggle';
       export { ToggleGroup } from './ToggleGroup';
       export { Tooltip } from './Tooltip';
       export { Tree } from './Tree';
       ```

    2. Rewrite `src/index.ts` — remove the entire Vue plugin install function, replace with clean named re-exports:
       ```typescript
       import '@/assets/main.css';

       // Components
       export * from './components';

       // Hooks (will be populated in Plan 04)
       // export { useControllable } from './hooks/useControllable';
       // export { useTransition } from './hooks/useTransition';
       ```

       Key changes from Vue version:
       - Removed `import type { App } from 'vue'`
       - Removed `import * as components from './components'`
       - Removed `import event from './utils/event'`
       - Removed entire `export default { install(app: App) { ... } }` block (Vue plugin, $confirm, $toast — moved to Phase 5 providers)
       - Removed OptionsConfirm and OptionsToast interfaces (moved to Phase 5)
       - Kept `import '@/assets/main.css'` for side-effect CSS import
       - Removed `export * from './composables'` (will be replaced by hooks export in Plan 04)
  </action>
  <verify>
    <automated>cd /Users/etus_0032/Desktop/AnaCarolina/design-system && grep -c "default" src/components/index.ts && grep -c "Plugin\|install\|vue" src/index.ts; echo "---"; grep -c "export" src/components/index.ts; echo "Expected: 0 default, 0 vue refs, 57 exports"</automated>
  </verify>
  <done>src/components/index.ts has 57 named exports (no default exports). src/index.ts is a clean re-export file with no Vue imports or plugin install.</done>
</task>

</tasks>

<verification>
- `grep -c "Plugin" src/components/*/index.ts` returns 0 for all files
- `grep -c "default" src/components/index.ts` returns 0
- `grep "vue" src/index.ts` returns no matches
- `ls src/components/*/*.tsx | wc -l` returns 57
- `npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "error"` returns 0 (or only errors from unconverted .vue references in non-index files)
</verification>

<success_criteria>
- All 57 component index.ts files use plain `export { X } from './X'` pattern
- All 57 components have stub .tsx files
- src/components/index.ts has 57 named re-exports
- src/index.ts re-exports from ./components without any Vue imports
- No Vue Plugin/install patterns remain in any index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
